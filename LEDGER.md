# Lane 3 ledger — whiteboard field (builder-3)

| # | Class | Entry |
|---|---|---|
| 1 | DECISION | No field registry exists on base (RecordPage.tsx:988 / DataTable.tsx:432 inline switches) → create `record-core/fields/` mirroring Lane 0's views registry; built-in types stay in their switches. Fan-out: every host consults the registry BEFORE its switch. |
| 2 | DECISION | Drop `@excalidraw/utils` (lane spec named it): published 0.1.2 is dated 2022-03-04, three years older than the 0.18 scene format; `@excalidraw/excalidraw` 0.18.1 exports `exportToSvg`/`getSceneVersion` itself (clone `packages/excalidraw/index.tsx:393,413`). One dep, one shared lazy chunk. Lead-approved (spec corrected). |
| 3 | CONSTRAINT | Lead arbitration: ONE registry, per-type entries carry BOTH render-side (L3: render/cell/previewText/layout/filterable/keyboardEditable/clearValue) and editor-side (L6: Draft/coerce/validate) optional slots. Merged type sent to builder-6 as the single §10 negotiation round. |
| 4 | DECISION | Value shape `{elements, appState?:{scrollX,scrollY,zoom}}`; saves gated by `getSceneVersion` (pans/selection never write → no toast/timeline churn); `UIOptions.tools.image:false` + `loadScene`/`saveToActiveFile` hidden in v1 (no base64 blobs in the command log, no file-system actions in an embedded field). |
| 5 | DECISION | Mobile (§4b): rest = static thumbnail + "Edit canvas" tap affordance; tap opens a fullscreen overlay (NOT vaul — drawer drag-to-dismiss conflicts with draw gestures); inline drag-draw declared desktop-first. Lead-approved. |
| 6 | REUSE | builder-1 inventory broadcast honored: `formatCell`/`csvCell`, `optionMeta`/`chipStyle`/`OptionChip`/`activeFields`, `normalizeOption`/`optionValues` imported, never re-extracted; registry hooks INTO `formatCell` rather than beside it. |
| 7 | DECISION | builder-4's `src/tokens/resolve.ts` (token→rgba, on feat/view-map) NOT consumed in v1: whiteboard paints no token literals — the §4-sanctioned binding for excalidraw is its `theme` prop, driven by a `data-theme` attribute observer on `<html>` (the exact mechanism App.tsx:377-380 toggles). Adoption path noted if canvas literals ever appear (post-merge, avoids a cross-branch dependency on an unmerged lane). |
| 8 | CONSTRAINT | Excalidraw embedding facts (measured via probes, encoded in journey helpers): toolbar tools are visually-hidden radio inputs (force-click the input; keyboard tool shortcuts never reach an embedded canvas); the properties island covers the canvas's left ~30% while a tool is armed (scripted draws stay right of x=0.4); mouse coords are viewport-relative (scroll the block into view + tall viewport before drawing). |
| 9 | CONSTRAINT | Export SVGs carry only a viewBox — CSS needs a DEFINITE height (ratio derives width); `max-height` alone collapses to 0x0 inside a shrink-to-fit inline context (playwright "not visible"). |
| 10 | DECISION | Viewport persistence DROPPED (evidence-driven): scene coords absorb the authoring-time canvas offset, so a stored scroll re-applied on a differently laid-out mount opens on empty space (seen on the demo seed's first render). Value = `{elements}` only; every mount `scrollToContent`; stored `appState` tolerated+ignored. Docs + seeds updated in the same pass. |
| 11 | DECISION | `@excalidraw/utils` npm-cache permission failures + scratch runs use a lane-local `--cache` dir (builder-1's footgun confirmed independently). Unit-test imports use explicit `.ts` specifiers (node strip-types), matching the view-registry test precedent. |
| 12 | REUSE | Demo + fixture scenes AUTHORED by scripted drawing in the real editor and captured from the API (3,427-byte 4-element scene; a 2,164-byte 2-element trim for gallery/kanban) — never hand-written elements. `server/seed.mjs` returns null for whiteboard in generated rows (canvases are drawn, not fabricated). |
