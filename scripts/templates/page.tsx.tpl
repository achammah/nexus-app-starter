import * as React from "react";

/* __LABEL__ — generated page (scripts/generate.mjs page __NAME__).
   Ordinary React: the vendored kit (src/ui/components/ui/*), primitives, tokens,
   and the /api client are all available. Edit freely — this file is yours. */

export function __NAME__() {
  return (
    <div className="nxCard" style={{ padding: 24 }}>
      <h2 style={{ margin: 0, font: "var(--nx-text-h2, inherit)" }}>__LABEL__</h2>
      <p style={{ color: "var(--nx-fg-muted)", marginTop: 8 }}>
        Generated page. Route: <code>#/p/__KEY__</code>. Replace this with the real surface.
      </p>
    </div>
  );
}
