# Feature/test manifest

One row per feature, written the wave it ships. Primitive = the platform primitive id behind it (`wf_…`/`task_…`/`agent_…`), or `mock` (labeled in the UI), or `local` for pure-UI features with no platform twin. A proof must be VISIBLE ("the API returns 200" is not visible). `Last verified` is written by `npm run journeys`, never by hand.

| Feature | Primitive | How to exercise (exact click path) | VISIBLE outcome that proves it | Last verified (local / hosted) |
|---|---|---|---|---|
| App shell + nav | local | open `/` → click each nav item | active nav item highlights; page title + row count change | local 2026-07-20T20:33 |
| Companies table | local | nav → Companies | 8 seeded rows render; header sort toggles order | local 2026-07-20T20:33 |
| Record page + inline edit | local | Companies → open “Brightline Analytics” → edit City → blur | field persists after reload; timeline gains an `updated` event; toast “Saved” | local 2026-07-20T20:33 |
| Notes | local | any record → Notes tab → type + Add | note appears in list; timeline gains a `note` event | local 2026-07-20T20:33 |
| Deals board (kanban) | local | nav → Deals (Board view) | 5 stage columns; changing a deal’s Stage moves its card to the target column | local 2026-07-20T20:33 |
| Create record | local | Companies → New company → name → Create | dialog closes; record page opens; count increments | local 2026-07-20T20:33 |
| Global search | local | topbar search → type “bright” → Enter | list filters to matching rows; filter chip shows the query | local 2026-07-20T20:33 |
| Theme toggle | local | topbar moon/sun | background/ink invert; choice survives reload | local 2026-07-20T20:33 |
| Data spine (app_state kv) | mock | POST /api/state {key,value} → GET /api/state | latest-per-key value returned (append-only history behind it) | local 2026-07-20T20:33 |
| Command palette (⌘K) | local | Ctrl/⌘-K → type a record name → Enter | palette lists live record hits across objects; selecting one opens its record page | local 2026-07-20T20:33 |
| Saved view (filter+view persist) | local | filter People to “maya” → visit Deals → return | the filter text AND its applied result survive navigation (per-object persistence) | local 2026-07-20T20:33 |
| Relation link cells | local | Deals table → click a Company cell | lands on Companies FILTERED to that company (1 row) | local 2026-07-20T20:33 |
| Bulk select · CSV export · reviewed delete | local | select rows → Export CSV / Delete | a .csv downloads; Delete opens a review dialog NAMING the records; confirm removes them and the count drops | local 2026-07-20T20:33 |
| Embedded agent chat dock | local | set chat.embedUrl in starter.config.json | floating chat button + iframe panel; unconfigured → nothing renders | local 2026-07-20T20:33 |
| Custom pages + kit demo | local | nav → Kit demo | the registered page renders; empty form submit shows a zod error; chart svg + sheet + accordion work live | local 2026-07-20T20:33 |
| Auth seam (login gate) | local | set AUTH_USERS + APP_SECRET → open the app | login card gates the shell; wrong password shows an error; right password enters; the API 401s without a session and unlocks with the cookie | local 2026-07-20T20:33 |
| Virtualized big lists | local | any object >80 rows | the DOM renders a WINDOW (row count < total) while scroll reaches every row; total count stays correct | local 2026-07-20T20:33 |
| Column visibility + sort persist | local | Columns menu → uncheck a field; sort a header; navigate away and back | the column disappears and STAYS hidden; the sort order survives navigation; re-checking restores | local 2026-07-20T20:33 |
| Date fields (calendar) | local | Deals table shows formatted dates → open a deal → Close date → pick a day | table cells render “14 Aug 2026”-style; the calendar pick saves, survives reload, and lands in the timeline | local 2026-07-20T20:33 |
| Select-field filter chips | local | Companies → Industry chip → check Software | list narrows to matching rows; the chip shows the active count; clear-all restores | local 2026-07-20T20:33 |
| Relation picker + related lists | local | open a company → related People/Deals panels; open a deal → Company picker | related panels list linked records (click opens them); the combobox sets the relation, saves, and the jump button lands filtered on the target | local 2026-07-20T20:33 |
| Building-blocks litmus (record-system class by config) | local | `CONFIG_PATH=journeys/fixtures/coverage.config.json npm run serve` | the SAME build assembles the hardest known topology (two-sided relations · staged pipeline · dates · scores) with zero code changes — a continuous TEST of block coverage, not a shipped template | local 2026-07-20T20:33 |
| User (assignee) fields | local | open a deal → Owner → pick from the directory | combobox over `users` from config; table cells render avatar + name; pick saves + persists | local 2026-07-20T20:33 |
| Multiselect (tags) fields | local | People table tag chips → Tags filter → record page toggle | chips in cells; contains-any filtering; toggling a tag saves, persists, and updates chips | local 2026-07-20T20:33 |
| Board group-by any field | local | any object w/ a select field → Board; deals board → “by …” picker → Owner | companies (no stageField) still gets a Board via Industry; deals board regroups into user columns; the choice persists per object | local 2026-07-20T20:33 |
| Attachments (files on records) | local | record → Files tab → Upload file | file lists with name + size + date; Download returns the exact bytes; timeline gains an “Attached …” event | local 2026-07-20T20:33 |
| Activity composer (call/email/meeting) | local | record → Timeline tab → pick kind → type → Log | activity appears at the top of the timeline with its kind icon; survives reload | local 2026-07-20T20:33 |
| AI-enrichment seam (field.primitive) | mock | company record → sparkle next to About | the field fills with a labeled (mock) value; timeline logs “Enriched … via Company research (mock)”; swap the /enrich mock in server/server.mjs for a real task/workflow call | local 2026-07-20T20:33 |
| Field-type validation (config-implied) | local | open a person → set Email to “not-an-email” → blur | toast carries the server’s message (“… valid email address”); the field reverts; the API 400s | local 2026-07-20T20:33 |
| Demo-data badge | local | boot with seeded config → look at the sidebar footer | a “Demo data” chip is visible whenever seeded fictional rows exist; delete the seed and it disappears | local 2026-07-20T20:33 |
| Image downscale on upload | local | record → Files → upload a large image | the stored file’s pixel width is capped at 1600 (verified by reading the PNG header of the downloaded bytes) | local 2026-07-20T20:33 |
| Chart view (group + measure) | local | any object w/ a select/user field → Chart; deals → “Σ Amount” | one bar per group option with value labels; measure switches Count ↔ field sums (deals New = 53,000); group/measure choices persist per object | local 2026-07-20T20:33 |
| Live sync (cross-viewer updates) | local | open a table; edit the same object from another tab/agent | the change appears in the open view within ~5s, no reload (per-object rev poll; pauses when the tab is hidden) | local 2026-07-20T20:33 |
| Generator CLI (object scaffolding) | local | `npm run generate object Gadget -- --fields "name:text:primary,status:select:A\|B"` | the config gains the object; the app boots with its table/board + seeded rows; `generate page`/`journey` scaffold from editable templates | local 2026-07-20T20:33 |
| Skin system (org brand as data) | local | set `theme.skinPreset` (or an inline `theme.skin` object) → reload | the WHOLE app re-brands from one small JSON: accent ramp, dark chrome shell, radius personality (vendored shadcn kit included), fonts, labels, semantic palette, logo mark/wordmark; dark mode derives from the same brand | local 2026-07-20T20:33 |
| Accounts (signup + email verification) | local | `AUTH_MODE=accounts` + `APP_SECRET` → Create an account → follow the mail link | signup enters the app; the verification mail lands in the dev outbox (`/api/outbox`); its link flips the account to verified | local 2026-07-20T20:33 |
| Password reset (anti-enumeration) + delete-by-confirmation | local | Forgot password? → mail link → new password; account deletion asks for a mailed confirmation | known and unknown addresses answer identically (decoy mail keeps timing parity); reset kills the old password AND old sessions; deletion only completes from the mailed token | local 2026-07-20T20:33 |
| Teams (invitations, join code, roles) | local | `/p/team` → create → invite by mail / share the join code | pending → active membership states are visible; the invite mail's link activates; duplicate invites 409 explicitly; the last owner can't be demoted | local 2026-07-20T20:33 |
| Permissions (config-declared, role-enforced) | local | give an object a `permissions` block → sign in as a plain member | create button and inline editors disappear for uncovered actions AND the API 403s them; owners keep the full surface | local 2026-07-20T20:33 |
| Webhooks (typed catalog, signed, logged) | local | `/p/webhooks` → URL + pick events → Create → mutate a record | the endpoint receives an HMAC-signed POST (verifiable with the once-shown secret); the per-endpoint delivery log shows the row; test-send button | local 2026-07-20T20:33 |
| Record subscriptions (@mentions notify watchers) | local | record header → Watch; Notes → type `@` | watchers get an outbox mail on notes/activities/updates (actor excluded); `@Name` autocompletes from the directory and auto-subscribes the mentioned account | local 2026-07-20T20:33 |
| Job seam (queue + recurring digest) | local | set `DIGEST_EVERY_MS` (+ optional `DIGEST_TO`) | the digest job writes per-object rollups into `app_state["digest:latest"]` on schedule; `/api/jobs` shows runs with status/retries | local 2026-07-20T20:33 |
| MCP server (AI assistant over the data model) | local | `claude mcp add my-app -- node scripts/mcp-server.mjs` (app running) | an assistant lists entities, reads schemas, queries live records and timelines through five read-only MCP tools | local 2026-07-20T20:33 |
| Theme editor (live, persisted) | local | `/p/theme` → pick a preset / brand color / corners / shell | edits apply INSTANTLY app-wide and survive reload (app_state); Export JSON emits the skin for the config | local 2026-07-20T20:33 |
| Feature flags (one flag: nav + page + API) | local | boot with `FEATURE_WEBHOOKS=0` | the nav entry disappears AND the API 404s — one env flag, all three enforcement points | local 2026-07-20T20:33 |
| Team-scoped objects (per-team visibility + roles) | local | `"teamScoped": true` on an object → sidebar team switcher | rows are stamped to the active team; other teams see an empty list and 404 by id; acting under a team you don't belong to is refused; roles resolve PER TEAM | local 2026-07-20T20:33 |
| Own-record grants (editOwn/deleteOwn) | local | grant `member: ["view","create","editOwn"]` | rows carry their creator; the creator edits their own rows, others 403; the record page renders read-only for foreign rows and editable for your own | local 2026-07-20T20:33 |
| Team activity log + invitation revocation | local | `/p/team` → Activity; remove a pending member | create/invite/join/role-change/remove/revoke are all logged with actors; revoking a pending invitation kills its mail token | local 2026-07-20T20:33 |
| Native warehouse spine (restart-survivable) | wf_native (Google Cloud connector) | set `WAREHOUSE=bigquery` + `NEXUS_API_KEY` + `WAREHOUSE_CREDENTIAL_ID` → use the app → restart it | every record/note/team/file action replays from the append-only events table with the SAME ids and timestamps — a restart loses nothing; without the env, the in-memory mock serves identically | local 2026-07-20T20:33 |
| Field-type depth (boolean/rating/dateTime/array/longText/json, colored options, unique, deactivation) | local | boot `journeys/fixtures/depth.config.json` → Vendors | colored option chips everywhere a select renders; boolean/rating edit inline; array adds free tags; unique fields 409 duplicates by name; `isActive:false` hides a field from every surface AND write-protects it while the data survives | local 2026-07-20T20:33 |
| Saved views (named, shareable, per-object) | local | Views menu → shape filters/layout → “Save current as view” | the view is server-persisted and listed for every workspace member; applying it restores layout AND filters; “All …” resets to the default state | local 2026-07-20T20:33 |
| Side peek (stacked record panel over the list) | local | click any row → panel opens over the list; open a related record inside it | the list stays visible behind; related records STACK (crumbs show the path); Escape steps back then closes; the root rides the URL (`?peek=`) so reload restores it; a promote button escapes to the full page | local 2026-07-20T20:33 |
| Record-to-record pagination (N of M through the open set) | local | open a peek from a list → header pager | "N of M" reflects the position in the exact set you opened from; prev/next step through it and wrap | local 2026-07-20T20:33 |
| Spreadsheet keyboard model (row → cell → edit, type-to-edit) | local | focus a table → ↑↓/jk · x select · Enter → cell · arrows · type | typing on a focused cell opens the editor SEEDED with the keystroke; Enter saves + steps down, Tab sideways; Escape climbs edit → cell → row → selection; Cmd/Ctrl+Enter peeks | local 2026-07-20T20:33 |
| Inline create inside the relation picker (no-match → create & attach) | local | record page → relation picker → type a name that doesn't exist | a "Create …" row appears; one click creates the record with just that title, attaches it, and opens it for field-by-field completion | local 2026-07-20T20:33 |
| Context actions in the command palette | local | ⌘K on a list vs on a record's full page | the palette leads with actions for what's on screen: New record / Open trash on lists; Add/remove favorite on records (with the panel open, ⌘K goes to the panel's actions page instead) | local 2026-07-20T20:33 |
| Trash (soft delete, restore, permanent destroy) | local | select rows → Delete → toolbar trash icon | deleted rows leave the list but sit in the Trash dialog; Restore brings one back intact; “Delete forever” removes it from the trash and the API 404s it | local 2026-07-20T20:33 |
| Unique collision restores the trashed twin (upsert semantic) | local | trash a record with a unique field → create a new one with the same value | the SAME record id returns (200, `_resurrected: true`) carrying the new data; a collision with a LIVE row still 400s | local 2026-07-20T20:33 |
| Merge duplicates (winner priority, relations re-pointed) | local | select 2–10 rows → Merge → pick the survivor | preview table shows the final value per field and WHERE inherited values come from; after merge one row remains, relations elsewhere point at it, losers sit in the trash | local 2026-07-20T20:33 |
| Favorites (personal pin shelf) | local | open a record → ★ Favorite | a Favorites section appears in the sidebar; its link deep-links back to the record; unpinning clears it | local 2026-07-20T20:33 |
| Destroy is a separate grant (delete ≠ destroy) | local | permissions grant `delete` but not `destroy` → open the trash as each role | the delete-holder sees Restore only; “Delete forever” renders solely for roles granted `destroy` (owners always); the API 403s the rest | local 2026-07-20T20:33 |
| Trash retention sweep (TRASH_RETENTION_DAYS) | local | set `TRASH_RETENTION_DAYS` → trash a row → wait a sweep | expired trash disappears on its own; the sweep shows up as a done `trash-sweep` job | local 2026-07-20T20:33 |
| Kanban per-column rollups (sum/avg/min/max) | local | deals Board → Rollup → “Sum of Amount” | every column head shows its rolled-up number (pipeline value per stage); the choice persists per object | local 2026-07-20T20:33 |
| Bulk edit (batched, live progress) | local | select rows → Edit → pick field + value → Update all | all selected rows take the value with a live N/M progress readout and cancel; empty value clears the field | local 2026-07-20T20:33 |
| CSV import wizard | local | any list → Import → paste CSV or pick a .csv → map columns → preview → run | column selects auto-map on name/label match; preview shows per-row verdicts with validator reasons; summary counts created/restored/skipped/failed; the list count rises; failed rows download as a CSV with a reason column; a unique value matching a trashed row restores it under its original id | local 2026-07-20T20:33 |
| API keys (role-scoped access) | local | /p/apikeys → name + role → Create → call the API with `x-api-key` | the full `nak_…` key renders exactly once; a viewer key reads rows but a PATCH with it 403s; revoking flips the row to revoked and the key answers 401; team-scoped objects refuse key callers | local 2026-07-20T20:33 |
| Multi-level sort (shift-click tie-breaker) | local | click one header, shift-click a second | rows sort by the first key with the second as tie-breaker; both levels persist in the saved state | local 2026-07-20T20:33 |
| Composite fields render (money/emails/phones/links/address/fullName) | local | `CONFIG_PATH=journeys/fixtures/depth.config.json npm run serve` → Vendors table + Sales reps board | money renders “€12,500” right-aligned; email/phone chips show first + “+N”; links anchor with the bare host (new tab); address one-lines “street, city”; a fullName primary titles the row link, record page and kanban card; a money rollup sums per column | local 2026-07-20T20:33 |
| Composite field editors (one-patch shaped saves) | local | open a vendor record → add an email, change the amount, set the city, change the last name | each edit saves as ONE patch (toast), survives reload, and the timeline logs readable old → new (“13750 EUR”, “…, Ghent”) | local 2026-07-20T20:33 |
| Composite validation (per-entry, field-named) | local | vendor record → type a bad email / bad URL / bad currency code into the editors | a visible inline error NAMES the field; the record keeps its old value; wrong-shaped API payloads 400 with the field named | local 2026-07-20T20:33 |
| Composite CSV flatten (shaped values in exports) | local | Vendors → select 2 rows → Export CSV | the downloaded file carries “12500 EUR”, “a; b” lists, the joined address/name — and a comma inside a URL stays one quoted column | local 2026-07-20T20:33 |
| Nav placement (config app.nav: top) | local | set `app.nav: "top"` in the config → reload (fixture: `journeys/fixtures/navtop.config.json`) | the nav renders as one horizontal top bar (brand, items with live counts, search/theme/sign-out on the right); no sidebar; clicking an item switches the list | local 2026-07-20T20:33 |
| Mobile nav drawer + full-screen peek | local | viewport ≤768px (side or top nav mode) → tap the burger | a left drawer lists objects/pages/favorites/team/sign-out; picking one navigates and closes it; an open record peek covers the full viewport; no horizontal page scroll at 390px | local 2026-07-20T20:33 |
| Possible duplicates panel (record page) | local | open a record that has a suspected twin | a "Possible duplicates" section lists each candidate WITH the matched rule ("Same name ignoring case…", "Same email"…) and announces "Review merge"; one click lands in the existing merge dialog preselected; Cancel keeps both records and leaves the pair selected | local 2026-07-20T20:33 |
| Duplicate sweep (find + review-merge) | local | any list → Find duplicates | a dialog groups suspected duplicates with member names + the rules that matched; per-group Review merge opens the merge dialog preselected; a clean object reports "No duplicates found."; nothing mutates without the merge confirmation | local 2026-07-20T20:33 |
| Relation identity (store ids, read labels) | local | depth fixture → open a vendor → pick a Sales rep → rename that rep | the picker lists fullName targets by joined name; the saved link is an ID (`_refs`); renaming the rep updates every inbound cell/section with NO sweep; the rep record shows the inverse-labeled “Vendors covered” section | local 2026-07-20T20:33 |
| Many-to-many relations (one committed diff on close) | local | vendor record → Certifications → toggle two options → Escape | toggles stay local while open; closing fires exactly ONE write with both ids; chips render both labels; reopening shows them checked; a chip’s × detaches just that link | local 2026-07-20T20:33 |
| Polymorphic relations (cross-type search, type labels) | local | vendor-note record → About → search “Atlas” | one search lists a vendor AND a rep with per-row type tags; picking each links the RIGHT identity (`_refs` carries object+id); the note lists on its target’s record under the poly field’s inverse label | local 2026-07-20T20:33 |
| Relation integrity (merge re-points ids; trash keeps, destroy severs) | local | merge two reps → trash/restore the winner → destroy a linked certification | after merge every inbound link points at the winner’s id; a trashed target keeps projecting its label and restore heals with zero writes; destroy removes the id from every pointing row | local 2026-07-20T20:33 |
| Composite-aware create dialog (shaped drafts + id-mode relations) | local | Sales reps → New → first/last + quota; Vendor notes → New → pick About | the dialog authors fullName + money as SHAPED values (code uppercased); the poly select is grouped by type and saves a typed ref that reads back as the target’s label | local 2026-07-20T20:33 |
| Relation identity replays (warehouse log, rename-safe) | mock | boot the depth fixture with the mock warehouse → label-writes + renames → restart | replayed state equals live state byte-for-byte: ids survive renames, labels re-project, a label written AFTER a rename stays dangling verbatim | local 2026-07-20T20:33 |
| Tasks page (buckets, filters, complete) | local | nav → Tasks → create tasks with due dates → complete one | tasks group into Overdue/Today/This week/Later buckets; completing one visibly moves it to Done (API stamps doneAt); the status filter narrows the visible rows | local 2026-07-20T20:33 |
| Tasks on records (link, complete, unlink) | local | open a record → Tasks card → add a task → complete it → unlink | the task appears in the record's Tasks card AND on the Tasks page with a chip back to the record; completing stamps "Task done: …" on the record timeline; unlinking removes it from the record but keeps the task | local 2026-07-20T20:33 |
| Task assignment mail | local | AUTH_MODE=accounts → create a task assigned to a signed-up user | the assignment mail ("Task assigned: …") lands in the dev outbox for the assignee — never for the actor | local 2026-07-20T20:33 |
| Panel search page (slash) | local | press `/` on any list → type a cross-object query | the panel opens on a search page with an always-focused input; results span objects with type labels; Enter pushes the hit onto the SAME panel (crumbs "Search › …") while the list behind stays untouched | local 2026-07-20T20:33 |
| Panel actions page (context actions) | local | open a record peek → the ⚡ header button (or Cmd/Ctrl+K while the panel is open) | an actions page stacks over the record listing the palette's context actions; running Favorite pins the record; "Search records" pushes the search page (3-deep crumbs); Escape walks the whole stack closed | local 2026-07-20T20:33 |
| Slash-key guard (grid + dialogs win) | local | focus a table cell, press `/`; open a dialog, press `/` | on a cell, `/` seeds the inline editor and no panel opens; with a dialog open, `/` types into the dialog; from ROW focus (navigation), `/` opens search | local 2026-07-20T20:33 |
| Runtime schema: add a field (colored select, everywhere on reload) | local | Schema page → Add field on an object → select type + colored options → Create | the commit reloads the app; the field appears on the schema page, as a table column, a filter chip, and a record-page editor; a saved value lands in the API and the timeline | local 2026-07-20T20:33 |
| Runtime schema: add an object (full CRUD + runtime relation) | local | Schema page → Add object → then Add field (relation) on it | nav grows the object; a row creates through its dialog; the runtime relation links by ID through the identity picker and projects the target's label; trash + restore round-trips | local 2026-07-20T20:33 |
| Runtime schema: retire + re-activate (data preserved) | local | Schema page → Retire on a field with data → Re-activate | the field leaves the table and record page while the API still returns its stored values and rejects writes (400); re-activation brings the values back intact | local 2026-07-20T20:33 |
| Runtime schema replays (deltas + data interleaved, restart byte-equal) | mock | mock warehouse → schema ops + dependent data writes → restart | after restart, the merged config AND every object's rows equal the pre-restart state byte-for-byte — deltas re-apply at their original position in the log | local 2026-07-20T20:33 |
| Runtime schema guard rails (every block names its reason) | local | Schema page → try a duplicate key, remove an in-use option, set unique over duplicates | each block surfaces a message NAMING the reason (the colliding key, the held option value, the duplicate values); type-change-with-data, key changes, and retiring the primary 400 with named reasons | local 2026-07-20T20:33 |
| UI gallery (catalog + live skin switching) | local | nav → Gallery → click a skin → reset | primitives, a live vendored subset, the full inventory (self-checking snapshot stamp), record-core minis and every field type render on ONE page; picking ember repaints it all instantly; reset restores the app skin; nothing persists (nx-skin-css untouched); the rest of the app is unaffected | local 2026-07-20T20:33 |
| Gallery record-core sandbox (local rows) | local | Gallery → Record core section → use the keyboard grid | the mini DataTable's full keyboard model works on LOCAL fictional rows (type-to-edit commits a visible value) with zero /api requests — the page works on an empty app | local 2026-07-20T20:33 |
| Settings tab shell | local | nav → Settings → click the About tab, then back to Writing rules | the active tab bar indicator moves and the body content swaps between the static About text and the rule list | local 2026-07-20T20:33 |
| Settings rules (EditableRuleList CRUD) | local | Settings → Writing rules → Add rule → edit it → toggle Active off → delete | @390px the add form lays out as a 2×2 grid; the new rule appears in the list, edits persist after Save, the row dims when Active is off, and deleting the last row shows the empty state | local 2026-07-20T20:33 |
| App shell + nav | local | open `/` → click each nav item | active nav item highlights; page title + row count change | local 2026-07-20T20:56 |
| Companies table | local | nav → Companies | 8 seeded rows render; header sort toggles order | local 2026-07-20T20:56 |
| Record page + inline edit | local | Companies → open “Brightline Analytics” → edit City → blur | field persists after reload; timeline gains an `updated` event; toast “Saved” | local 2026-07-20T20:56 |
| Notes | local | any record → Notes tab → type + Add | note appears in list; timeline gains a `note` event | local 2026-07-20T20:56 |
| Deals board (kanban) | local | nav → Deals (Board view) | 5 stage columns; changing a deal’s Stage moves its card to the target column | local 2026-07-20T20:56 |
| Create record | local | Companies → New company → name → Create | dialog closes; record page opens; count increments | local 2026-07-20T20:56 |
| Global search | local | topbar search → type “bright” → Enter | list filters to matching rows; filter chip shows the query | local 2026-07-20T20:56 |
| Theme toggle | local | topbar moon/sun | background/ink invert; choice survives reload | local 2026-07-20T20:56 |
| Data spine (app_state kv) | mock | POST /api/state {key,value} → GET /api/state | latest-per-key value returned (append-only history behind it) | local 2026-07-20T20:56 |
| Command palette (⌘K) | local | Ctrl/⌘-K → type a record name → Enter | palette lists live record hits across objects; selecting one opens its record page | local 2026-07-20T20:56 |
| Saved view (filter+view persist) | local | filter People to “maya” → visit Deals → return | the filter text AND its applied result survive navigation (per-object persistence) | local 2026-07-20T20:56 |
| Relation link cells | local | Deals table → click a Company cell | lands on Companies FILTERED to that company (1 row) | local 2026-07-20T20:56 |
| Bulk select · CSV export · reviewed delete | local | select rows → Export CSV / Delete | a .csv downloads; Delete opens a review dialog NAMING the records; confirm removes them and the count drops | local 2026-07-20T20:56 |
| Embedded agent chat dock | local | set chat.embedUrl in starter.config.json | floating chat button + iframe panel; unconfigured → nothing renders | local 2026-07-20T20:56 |
| Custom pages + kit demo | local | nav → Kit demo | the registered page renders; empty form submit shows a zod error; chart svg + sheet + accordion work live | local 2026-07-20T20:56 |
| Auth seam (login gate) | local | set AUTH_USERS + APP_SECRET → open the app | login card gates the shell; wrong password shows an error; right password enters; the API 401s without a session and unlocks with the cookie | local 2026-07-20T20:56 |
| Virtualized big lists | local | any object >80 rows | the DOM renders a WINDOW (row count < total) while scroll reaches every row; total count stays correct | local 2026-07-20T20:56 |
| Column visibility + sort persist | local | Columns menu → uncheck a field; sort a header; navigate away and back | the column disappears and STAYS hidden; the sort order survives navigation; re-checking restores | local 2026-07-20T20:56 |
| Date fields (calendar) | local | Deals table shows formatted dates → open a deal → Close date → pick a day | table cells render “14 Aug 2026”-style; the calendar pick saves, survives reload, and lands in the timeline | local 2026-07-20T20:56 |
| Select-field filter chips | local | Companies → Industry chip → check Software | list narrows to matching rows; the chip shows the active count; clear-all restores | local 2026-07-20T20:56 |
| Relation picker + related lists | local | open a company → related People/Deals panels; open a deal → Company picker | related panels list linked records (click opens them); the combobox sets the relation, saves, and the jump button lands filtered on the target | local 2026-07-20T20:56 |
| Building-blocks litmus (record-system class by config) | local | `CONFIG_PATH=journeys/fixtures/coverage.config.json npm run serve` | the SAME build assembles the hardest known topology (two-sided relations · staged pipeline · dates · scores) with zero code changes — a continuous TEST of block coverage, not a shipped template | local 2026-07-20T20:56 |
| User (assignee) fields | local | open a deal → Owner → pick from the directory | combobox over `users` from config; table cells render avatar + name; pick saves + persists | local 2026-07-20T20:56 |
| Multiselect (tags) fields | local | People table tag chips → Tags filter → record page toggle | chips in cells; contains-any filtering; toggling a tag saves, persists, and updates chips | local 2026-07-20T20:56 |
| Board group-by any field | local | any object w/ a select field → Board; deals board → “by …” picker → Owner | companies (no stageField) still gets a Board via Industry; deals board regroups into user columns; the choice persists per object | local 2026-07-20T20:56 |
| Attachments (files on records) | local | record → Files tab → Upload file | file lists with name + size + date; Download returns the exact bytes; timeline gains an “Attached …” event | local 2026-07-20T20:56 |
| Activity composer (call/email/meeting) | local | record → Timeline tab → pick kind → type → Log | activity appears at the top of the timeline with its kind icon; survives reload | local 2026-07-20T20:56 |
| AI-enrichment seam (field.primitive) | mock | company record → sparkle next to About | the field fills with a labeled (mock) value; timeline logs “Enriched … via Company research (mock)”; swap the /enrich mock in server/server.mjs for a real task/workflow call | local 2026-07-20T20:56 |
| Field-type validation (config-implied) | local | open a person → set Email to “not-an-email” → blur | toast carries the server’s message (“… valid email address”); the field reverts; the API 400s | local 2026-07-20T20:56 |
| Demo-data badge | local | boot with seeded config → look at the sidebar footer | a “Demo data” chip is visible whenever seeded fictional rows exist; delete the seed and it disappears | local 2026-07-20T20:56 |
| Image downscale on upload | local | record → Files → upload a large image | the stored file’s pixel width is capped at 1600 (verified by reading the PNG header of the downloaded bytes) | local 2026-07-20T20:56 |
| Chart view (group + measure) | local | any object w/ a select/user field → Chart; deals → “Σ Amount” | one bar per group option with value labels; measure switches Count ↔ field sums (deals New = 53,000); group/measure choices persist per object | local 2026-07-20T20:56 |
| Live sync (cross-viewer updates) | local | open a table; edit the same object from another tab/agent | the change appears in the open view within ~5s, no reload (per-object rev poll; pauses when the tab is hidden) | local 2026-07-20T20:56 |
| Generator CLI (object scaffolding) | local | `npm run generate object Gadget -- --fields "name:text:primary,status:select:A\|B"` | the config gains the object; the app boots with its table/board + seeded rows; `generate page`/`journey` scaffold from editable templates | local 2026-07-20T20:56 |
| Skin system (org brand as data) | local | set `theme.skinPreset` (or an inline `theme.skin` object) → reload | the WHOLE app re-brands from one small JSON: accent ramp, dark chrome shell, radius personality (vendored shadcn kit included), fonts, labels, semantic palette, logo mark/wordmark; dark mode derives from the same brand | local 2026-07-20T20:56 |
| Accounts (signup + email verification) | local | `AUTH_MODE=accounts` + `APP_SECRET` → Create an account → follow the mail link | signup enters the app; the verification mail lands in the dev outbox (`/api/outbox`); its link flips the account to verified | local 2026-07-20T20:56 |
| Password reset (anti-enumeration) + delete-by-confirmation | local | Forgot password? → mail link → new password; account deletion asks for a mailed confirmation | known and unknown addresses answer identically (decoy mail keeps timing parity); reset kills the old password AND old sessions; deletion only completes from the mailed token | local 2026-07-20T20:56 |
| Teams (invitations, join code, roles) | local | `/p/team` → create → invite by mail / share the join code | pending → active membership states are visible; the invite mail's link activates; duplicate invites 409 explicitly; the last owner can't be demoted | local 2026-07-20T20:56 |
| Permissions (config-declared, role-enforced) | local | give an object a `permissions` block → sign in as a plain member | create button and inline editors disappear for uncovered actions AND the API 403s them; owners keep the full surface | local 2026-07-20T20:56 |
| Webhooks (typed catalog, signed, logged) | local | `/p/webhooks` → URL + pick events → Create → mutate a record | the endpoint receives an HMAC-signed POST (verifiable with the once-shown secret); the per-endpoint delivery log shows the row; test-send button | local 2026-07-20T20:56 |
| Record subscriptions (@mentions notify watchers) | local | record header → Watch; Notes → type `@` | watchers get an outbox mail on notes/activities/updates (actor excluded); `@Name` autocompletes from the directory and auto-subscribes the mentioned account | local 2026-07-20T20:56 |
| Job seam (queue + recurring digest) | local | set `DIGEST_EVERY_MS` (+ optional `DIGEST_TO`) | the digest job writes per-object rollups into `app_state["digest:latest"]` on schedule; `/api/jobs` shows runs with status/retries | local 2026-07-20T20:56 |
| MCP server (AI assistant over the data model) | local | `claude mcp add my-app -- node scripts/mcp-server.mjs` (app running) | an assistant lists entities, reads schemas, queries live records and timelines through five read-only MCP tools | local 2026-07-20T20:56 |
| Theme editor (live, persisted) | local | `/p/theme` → pick a preset / brand color / corners / shell | edits apply INSTANTLY app-wide and survive reload (app_state); Export JSON emits the skin for the config | local 2026-07-20T20:56 |
| Feature flags (one flag: nav + page + API) | local | boot with `FEATURE_WEBHOOKS=0` | the nav entry disappears AND the API 404s — one env flag, all three enforcement points | local 2026-07-20T20:56 |
| Team-scoped objects (per-team visibility + roles) | local | `"teamScoped": true` on an object → sidebar team switcher | rows are stamped to the active team; other teams see an empty list and 404 by id; acting under a team you don't belong to is refused; roles resolve PER TEAM | local 2026-07-20T20:56 |
| Own-record grants (editOwn/deleteOwn) | local | grant `member: ["view","create","editOwn"]` | rows carry their creator; the creator edits their own rows, others 403; the record page renders read-only for foreign rows and editable for your own | local 2026-07-20T20:56 |
| Team activity log + invitation revocation | local | `/p/team` → Activity; remove a pending member | create/invite/join/role-change/remove/revoke are all logged with actors; revoking a pending invitation kills its mail token | local 2026-07-20T20:56 |
| Native warehouse spine (restart-survivable) | wf_native (Google Cloud connector) | set `WAREHOUSE=bigquery` + `NEXUS_API_KEY` + `WAREHOUSE_CREDENTIAL_ID` → use the app → restart it | every record/note/team/file action replays from the append-only events table with the SAME ids and timestamps — a restart loses nothing; without the env, the in-memory mock serves identically | local 2026-07-20T20:56 |
| Field-type depth (boolean/rating/dateTime/array/longText/json, colored options, unique, deactivation) | local | boot `journeys/fixtures/depth.config.json` → Vendors | colored option chips everywhere a select renders; boolean/rating edit inline; array adds free tags; unique fields 409 duplicates by name; `isActive:false` hides a field from every surface AND write-protects it while the data survives | local 2026-07-20T20:56 |
| Saved views (named, shareable, per-object) | local | Views menu → shape filters/layout → “Save current as view” | the view is server-persisted and listed for every workspace member; applying it restores layout AND filters; “All …” resets to the default state | local 2026-07-20T20:56 |
| Side peek (stacked record panel over the list) | local | click any row → panel opens over the list; open a related record inside it | the list stays visible behind; related records STACK (crumbs show the path); Escape steps back then closes; the root rides the URL (`?peek=`) so reload restores it; a promote button escapes to the full page | local 2026-07-20T20:56 |
| Record-to-record pagination (N of M through the open set) | local | open a peek from a list → header pager | "N of M" reflects the position in the exact set you opened from; prev/next step through it and wrap | local 2026-07-20T20:56 |
| Spreadsheet keyboard model (row → cell → edit, type-to-edit) | local | focus a table → ↑↓/jk · x select · Enter → cell · arrows · type | typing on a focused cell opens the editor SEEDED with the keystroke; Enter saves + steps down, Tab sideways; Escape climbs edit → cell → row → selection; Cmd/Ctrl+Enter peeks | local 2026-07-20T20:56 |
| Inline create inside the relation picker (no-match → create & attach) | local | record page → relation picker → type a name that doesn't exist | a "Create …" row appears; one click creates the record with just that title, attaches it, and opens it for field-by-field completion | local 2026-07-20T20:56 |
| Context actions in the command palette | local | ⌘K on a list vs on a record's full page | the palette leads with actions for what's on screen: New record / Open trash on lists; Add/remove favorite on records (with the panel open, ⌘K goes to the panel's actions page instead) | local 2026-07-20T20:56 |
| Trash (soft delete, restore, permanent destroy) | local | select rows → Delete → toolbar trash icon | deleted rows leave the list but sit in the Trash dialog; Restore brings one back intact; “Delete forever” removes it from the trash and the API 404s it | local 2026-07-20T20:56 |
| Unique collision restores the trashed twin (upsert semantic) | local | trash a record with a unique field → create a new one with the same value | the SAME record id returns (200, `_resurrected: true`) carrying the new data; a collision with a LIVE row still 400s | local 2026-07-20T20:56 |
| Merge duplicates (winner priority, relations re-pointed) | local | select 2–10 rows → Merge → pick the survivor | preview table shows the final value per field and WHERE inherited values come from; after merge one row remains, relations elsewhere point at it, losers sit in the trash | local 2026-07-20T20:56 |
| Favorites (personal pin shelf) | local | open a record → ★ Favorite | a Favorites section appears in the sidebar; its link deep-links back to the record; unpinning clears it | local 2026-07-20T20:56 |
| Destroy is a separate grant (delete ≠ destroy) | local | permissions grant `delete` but not `destroy` → open the trash as each role | the delete-holder sees Restore only; “Delete forever” renders solely for roles granted `destroy` (owners always); the API 403s the rest | local 2026-07-20T20:56 |
| Trash retention sweep (TRASH_RETENTION_DAYS) | local | set `TRASH_RETENTION_DAYS` → trash a row → wait a sweep | expired trash disappears on its own; the sweep shows up as a done `trash-sweep` job | local 2026-07-20T20:56 |
| Kanban per-column rollups (sum/avg/min/max) | local | deals Board → Rollup → “Sum of Amount” | every column head shows its rolled-up number (pipeline value per stage); the choice persists per object | local 2026-07-20T20:56 |
| Bulk edit (batched, live progress) | local | select rows → Edit → pick field + value → Update all | all selected rows take the value with a live N/M progress readout and cancel; empty value clears the field | local 2026-07-20T20:56 |
| CSV import wizard | local | any list → Import → paste CSV or pick a .csv → map columns → preview → run | column selects auto-map on name/label match; preview shows per-row verdicts with validator reasons; summary counts created/restored/skipped/failed; the list count rises; failed rows download as a CSV with a reason column; a unique value matching a trashed row restores it under its original id | local 2026-07-20T20:56 |
| API keys (role-scoped access) | local | /p/apikeys → name + role → Create → call the API with `x-api-key` | the full `nak_…` key renders exactly once; a viewer key reads rows but a PATCH with it 403s; revoking flips the row to revoked and the key answers 401; team-scoped objects refuse key callers | local 2026-07-20T20:56 |
| Multi-level sort (shift-click tie-breaker) | local | click one header, shift-click a second | rows sort by the first key with the second as tie-breaker; both levels persist in the saved state | local 2026-07-20T20:56 |
| Composite fields render (money/emails/phones/links/address/fullName) | local | `CONFIG_PATH=journeys/fixtures/depth.config.json npm run serve` → Vendors table + Sales reps board | money renders “€12,500” right-aligned; email/phone chips show first + “+N”; links anchor with the bare host (new tab); address one-lines “street, city”; a fullName primary titles the row link, record page and kanban card; a money rollup sums per column | local 2026-07-20T20:56 |
| Composite field editors (one-patch shaped saves) | local | open a vendor record → add an email, change the amount, set the city, change the last name | each edit saves as ONE patch (toast), survives reload, and the timeline logs readable old → new (“13750 EUR”, “…, Ghent”) | local 2026-07-20T20:56 |
| Composite validation (per-entry, field-named) | local | vendor record → type a bad email / bad URL / bad currency code into the editors | a visible inline error NAMES the field; the record keeps its old value; wrong-shaped API payloads 400 with the field named | local 2026-07-20T20:56 |
| Composite CSV flatten (shaped values in exports) | local | Vendors → select 2 rows → Export CSV | the downloaded file carries “12500 EUR”, “a; b” lists, the joined address/name — and a comma inside a URL stays one quoted column | local 2026-07-20T20:56 |
| Nav placement (config app.nav: top) | local | set `app.nav: "top"` in the config → reload (fixture: `journeys/fixtures/navtop.config.json`) | the nav renders as one horizontal top bar (brand, items with live counts, search/theme/sign-out on the right); no sidebar; clicking an item switches the list | local 2026-07-20T20:56 |
| Mobile nav drawer + full-screen peek | local | viewport ≤768px (side or top nav mode) → tap the burger | a left drawer lists objects/pages/favorites/team/sign-out; picking one navigates and closes it; an open record peek covers the full viewport; no horizontal page scroll at 390px | local 2026-07-20T20:56 |
| Possible duplicates panel (record page) | local | open a record that has a suspected twin | a "Possible duplicates" section lists each candidate WITH the matched rule ("Same name ignoring case…", "Same email"…) and announces "Review merge"; one click lands in the existing merge dialog preselected; Cancel keeps both records and leaves the pair selected | local 2026-07-20T20:56 |
| Duplicate sweep (find + review-merge) | local | any list → Find duplicates | a dialog groups suspected duplicates with member names + the rules that matched; per-group Review merge opens the merge dialog preselected; a clean object reports "No duplicates found."; nothing mutates without the merge confirmation | local 2026-07-20T20:56 |
| Relation identity (store ids, read labels) | local | depth fixture → open a vendor → pick a Sales rep → rename that rep | the picker lists fullName targets by joined name; the saved link is an ID (`_refs`); renaming the rep updates every inbound cell/section with NO sweep; the rep record shows the inverse-labeled “Vendors covered” section | local 2026-07-20T20:56 |
| Many-to-many relations (one committed diff on close) | local | vendor record → Certifications → toggle two options → Escape | toggles stay local while open; closing fires exactly ONE write with both ids; chips render both labels; reopening shows them checked; a chip’s × detaches just that link | local 2026-07-20T20:56 |
| Polymorphic relations (cross-type search, type labels) | local | vendor-note record → About → search “Atlas” | one search lists a vendor AND a rep with per-row type tags; picking each links the RIGHT identity (`_refs` carries object+id); the note lists on its target’s record under the poly field’s inverse label | local 2026-07-20T20:56 |
| Relation integrity (merge re-points ids; trash keeps, destroy severs) | local | merge two reps → trash/restore the winner → destroy a linked certification | after merge every inbound link points at the winner’s id; a trashed target keeps projecting its label and restore heals with zero writes; destroy removes the id from every pointing row | local 2026-07-20T20:56 |
| Composite-aware create dialog (shaped drafts + id-mode relations) | local | Sales reps → New → first/last + quota; Vendor notes → New → pick About | the dialog authors fullName + money as SHAPED values (code uppercased); the poly select is grouped by type and saves a typed ref that reads back as the target’s label | local 2026-07-20T20:56 |
| Relation identity replays (warehouse log, rename-safe) | mock | boot the depth fixture with the mock warehouse → label-writes + renames → restart | replayed state equals live state byte-for-byte: ids survive renames, labels re-project, a label written AFTER a rename stays dangling verbatim | local 2026-07-20T20:56 |
| Tasks page (buckets, filters, complete) | local | nav → Tasks → create tasks with due dates → complete one | tasks group into Overdue/Today/This week/Later buckets; completing one visibly moves it to Done (API stamps doneAt); the status filter narrows the visible rows | local 2026-07-20T20:56 |
| Tasks on records (link, complete, unlink) | local | open a record → Tasks card → add a task → complete it → unlink | the task appears in the record's Tasks card AND on the Tasks page with a chip back to the record; completing stamps "Task done: …" on the record timeline; unlinking removes it from the record but keeps the task | local 2026-07-20T20:56 |
| Task assignment mail | local | AUTH_MODE=accounts → create a task assigned to a signed-up user | the assignment mail ("Task assigned: …") lands in the dev outbox for the assignee — never for the actor | local 2026-07-20T20:56 |
| Panel search page (slash) | local | press `/` on any list → type a cross-object query | the panel opens on a search page with an always-focused input; results span objects with type labels; Enter pushes the hit onto the SAME panel (crumbs "Search › …") while the list behind stays untouched | local 2026-07-20T20:56 |
| Panel actions page (context actions) | local | open a record peek → the ⚡ header button (or Cmd/Ctrl+K while the panel is open) | an actions page stacks over the record listing the palette's context actions; running Favorite pins the record; "Search records" pushes the search page (3-deep crumbs); Escape walks the whole stack closed | local 2026-07-20T20:56 |
| Slash-key guard (grid + dialogs win) | local | focus a table cell, press `/`; open a dialog, press `/` | on a cell, `/` seeds the inline editor and no panel opens; with a dialog open, `/` types into the dialog; from ROW focus (navigation), `/` opens search | local 2026-07-20T20:56 |
| Runtime schema: add a field (colored select, everywhere on reload) | local | Schema page → Add field on an object → select type + colored options → Create | the commit reloads the app; the field appears on the schema page, as a table column, a filter chip, and a record-page editor; a saved value lands in the API and the timeline | local 2026-07-20T20:56 |
| Runtime schema: add an object (full CRUD + runtime relation) | local | Schema page → Add object → then Add field (relation) on it | nav grows the object; a row creates through its dialog; the runtime relation links by ID through the identity picker and projects the target's label; trash + restore round-trips | local 2026-07-20T20:56 |
| Runtime schema: retire + re-activate (data preserved) | local | Schema page → Retire on a field with data → Re-activate | the field leaves the table and record page while the API still returns its stored values and rejects writes (400); re-activation brings the values back intact | local 2026-07-20T20:56 |
| Runtime schema replays (deltas + data interleaved, restart byte-equal) | mock | mock warehouse → schema ops + dependent data writes → restart | after restart, the merged config AND every object's rows equal the pre-restart state byte-for-byte — deltas re-apply at their original position in the log | local 2026-07-20T20:56 |
| Runtime schema guard rails (every block names its reason) | local | Schema page → try a duplicate key, remove an in-use option, set unique over duplicates | each block surfaces a message NAMING the reason (the colliding key, the held option value, the duplicate values); type-change-with-data, key changes, and retiring the primary 400 with named reasons | local 2026-07-20T20:56 |
| UI gallery (catalog + live skin switching) | local | nav → Gallery → click a skin → reset | primitives, a live vendored subset, the full inventory (self-checking snapshot stamp), record-core minis and every field type render on ONE page; picking ember repaints it all instantly; reset restores the app skin; nothing persists (nx-skin-css untouched); the rest of the app is unaffected | local 2026-07-20T20:56 |
| Gallery record-core sandbox (local rows) | local | Gallery → Record core section → use the keyboard grid | the mini DataTable's full keyboard model works on LOCAL fictional rows (type-to-edit commits a visible value) with zero /api requests — the page works on an empty app | local 2026-07-20T20:56 |
| AI field enrichment (runAiTask + ThinkingDots) | mock (task, via `field.primitive.taskId`) | boot `journeys/fixtures/foundations.config.json` + `NEXUS_API_KEY` + a mock Nexus base → open a record → click the enrich (Sparkles) affordance | the affordance becomes a ThinkingDots running indicator while the AI task runs, then the field fills with the task's output (mock returns a marker); with no `taskId`/key the mock-value path is byte-unchanged | local 2026-07-20T20:56 |
| External-writer sync (RemoteStore.sync + warehouse loadSince) | mock (BigQuery seam) | boot `WAREHOUSE=bigquery` + a mock warehouse → an external writer appends a `patch` event → POST /api/sync | the externally-written value appears in the list after the sync pulls the event in (no restart) — RemoteStore.sync() + warehouse loadSince/_read end to end | local 2026-07-20T20:56 |
| Async hook cores (computeAsyncOp stall, createDebouncer coalesce) | local | node-level unit (injectable clock) — `npm run journeys` runs it | the stall flag flips only past the threshold; 3 rapid debounced triggers coalesce to ONE persist ending in `saved`. UNIT-tested (no foundations UI); VISIBLE consumer: editor autosave state + copilot poll (wave 2) | local 2026-07-20T20:56 |
| Async server helpers (emulatorChat, fireAsyncGeneration) | mock (Nexus seam) | node-level seam against a mock Nexus boundary — `npm run journeys` runs it | emulatorChat returns the agent reply + invoked tools through the session proxy; fireAsyncGeneration creates the placeholder row and fires the webhook with the created id folded in. SEAM-tested (no foundations UI); VISIBLE consumer: copilot panel + generation wizard (wave 2) | local 2026-07-20T20:56 |
| App shell + nav | local | open `/` → click each nav item | active nav item highlights; page title + row count change | local 2026-07-20T20:53 |
| Companies table | local | nav → Companies | 8 seeded rows render; header sort toggles order | local 2026-07-20T20:53 |
| Record page + inline edit | local | Companies → open “Brightline Analytics” → edit City → blur | field persists after reload; timeline gains an `updated` event; toast “Saved” | local 2026-07-20T20:53 |
| Notes | local | any record → Notes tab → type + Add | note appears in list; timeline gains a `note` event | local 2026-07-20T20:53 |
| Deals board (kanban) | local | nav → Deals (Board view) | 5 stage columns; changing a deal’s Stage moves its card to the target column | local 2026-07-20T20:53 |
| Create record | local | Companies → New company → name → Create | dialog closes; record page opens; count increments | local 2026-07-20T20:53 |
| Global search | local | topbar search → type “bright” → Enter | list filters to matching rows; filter chip shows the query | local 2026-07-20T20:53 |
| Theme toggle | local | topbar moon/sun | background/ink invert; choice survives reload | local 2026-07-20T20:53 |
| Data spine (app_state kv) | mock | POST /api/state {key,value} → GET /api/state | latest-per-key value returned (append-only history behind it) | local 2026-07-20T20:53 |
| Command palette (⌘K) | local | Ctrl/⌘-K → type a record name → Enter | palette lists live record hits across objects; selecting one opens its record page | local 2026-07-20T20:53 |
| Saved view (filter+view persist) | local | filter People to “maya” → visit Deals → return | the filter text AND its applied result survive navigation (per-object persistence) | local 2026-07-20T20:53 |
| Relation link cells | local | Deals table → click a Company cell | lands on Companies FILTERED to that company (1 row) | local 2026-07-20T20:53 |
| Bulk select · CSV export · reviewed delete | local | select rows → Export CSV / Delete | a .csv downloads; Delete opens a review dialog NAMING the records; confirm removes them and the count drops | local 2026-07-20T20:53 |
| Embedded agent chat dock | local | set chat.embedUrl in starter.config.json | floating chat button + iframe panel; unconfigured → nothing renders | local 2026-07-20T20:53 |
| Custom pages + kit demo | local | nav → Kit demo | the registered page renders; empty form submit shows a zod error; chart svg + sheet + accordion work live | local 2026-07-20T20:53 |
| Auth seam (login gate) | local | set AUTH_USERS + APP_SECRET → open the app | login card gates the shell; wrong password shows an error; right password enters; the API 401s without a session and unlocks with the cookie | local 2026-07-20T20:53 |
| Virtualized big lists | local | any object >80 rows | the DOM renders a WINDOW (row count < total) while scroll reaches every row; total count stays correct | local 2026-07-20T20:53 |
| Column visibility + sort persist | local | Columns menu → uncheck a field; sort a header; navigate away and back | the column disappears and STAYS hidden; the sort order survives navigation; re-checking restores | local 2026-07-20T20:53 |
| Date fields (calendar) | local | Deals table shows formatted dates → open a deal → Close date → pick a day | table cells render “14 Aug 2026”-style; the calendar pick saves, survives reload, and lands in the timeline | local 2026-07-20T20:53 |
| Select-field filter chips | local | Companies → Industry chip → check Software | list narrows to matching rows; the chip shows the active count; clear-all restores | local 2026-07-20T20:53 |
| Relation picker + related lists | local | open a company → related People/Deals panels; open a deal → Company picker | related panels list linked records (click opens them); the combobox sets the relation, saves, and the jump button lands filtered on the target | local 2026-07-20T20:53 |
| Building-blocks litmus (record-system class by config) | local | `CONFIG_PATH=journeys/fixtures/coverage.config.json npm run serve` | the SAME build assembles the hardest known topology (two-sided relations · staged pipeline · dates · scores) with zero code changes — a continuous TEST of block coverage, not a shipped template | local 2026-07-20T20:53 |
| User (assignee) fields | local | open a deal → Owner → pick from the directory | combobox over `users` from config; table cells render avatar + name; pick saves + persists | local 2026-07-20T20:53 |
| Multiselect (tags) fields | local | People table tag chips → Tags filter → record page toggle | chips in cells; contains-any filtering; toggling a tag saves, persists, and updates chips | local 2026-07-20T20:53 |
| Board group-by any field | local | any object w/ a select field → Board; deals board → “by …” picker → Owner | companies (no stageField) still gets a Board via Industry; deals board regroups into user columns; the choice persists per object | local 2026-07-20T20:53 |
| Attachments (files on records) | local | record → Files tab → Upload file | file lists with name + size + date; Download returns the exact bytes; timeline gains an “Attached …” event | local 2026-07-20T20:53 |
| Activity composer (call/email/meeting) | local | record → Timeline tab → pick kind → type → Log | activity appears at the top of the timeline with its kind icon; survives reload | local 2026-07-20T20:53 |
| AI-enrichment seam (field.primitive) | mock | company record → sparkle next to About | the field fills with a labeled (mock) value; timeline logs “Enriched … via Company research (mock)”; swap the /enrich mock in server/server.mjs for a real task/workflow call | local 2026-07-20T20:53 |
| Field-type validation (config-implied) | local | open a person → set Email to “not-an-email” → blur | toast carries the server’s message (“… valid email address”); the field reverts; the API 400s | local 2026-07-20T20:53 |
| Demo-data badge | local | boot with seeded config → look at the sidebar footer | a “Demo data” chip is visible whenever seeded fictional rows exist; delete the seed and it disappears | local 2026-07-20T20:53 |
| Image downscale on upload | local | record → Files → upload a large image | the stored file’s pixel width is capped at 1600 (verified by reading the PNG header of the downloaded bytes) | local 2026-07-20T20:53 |
| Chart view (group + measure) | local | any object w/ a select/user field → Chart; deals → “Σ Amount” | one bar per group option with value labels; measure switches Count ↔ field sums (deals New = 53,000); group/measure choices persist per object | local 2026-07-20T20:53 |
| Live sync (cross-viewer updates) | local | open a table; edit the same object from another tab/agent | the change appears in the open view within ~5s, no reload (per-object rev poll; pauses when the tab is hidden) | local 2026-07-20T20:53 |
| Generator CLI (object scaffolding) | local | `npm run generate object Gadget -- --fields "name:text:primary,status:select:A\|B"` | the config gains the object; the app boots with its table/board + seeded rows; `generate page`/`journey` scaffold from editable templates | local 2026-07-20T20:53 |
| Skin system (org brand as data) | local | set `theme.skinPreset` (or an inline `theme.skin` object) → reload | the WHOLE app re-brands from one small JSON: accent ramp, dark chrome shell, radius personality (vendored shadcn kit included), fonts, labels, semantic palette, logo mark/wordmark; dark mode derives from the same brand | local 2026-07-20T20:53 |
| Accounts (signup + email verification) | local | `AUTH_MODE=accounts` + `APP_SECRET` → Create an account → follow the mail link | signup enters the app; the verification mail lands in the dev outbox (`/api/outbox`); its link flips the account to verified | local 2026-07-20T20:53 |
| Password reset (anti-enumeration) + delete-by-confirmation | local | Forgot password? → mail link → new password; account deletion asks for a mailed confirmation | known and unknown addresses answer identically (decoy mail keeps timing parity); reset kills the old password AND old sessions; deletion only completes from the mailed token | local 2026-07-20T20:53 |
| Teams (invitations, join code, roles) | local | `/p/team` → create → invite by mail / share the join code | pending → active membership states are visible; the invite mail's link activates; duplicate invites 409 explicitly; the last owner can't be demoted | local 2026-07-20T20:53 |
| Permissions (config-declared, role-enforced) | local | give an object a `permissions` block → sign in as a plain member | create button and inline editors disappear for uncovered actions AND the API 403s them; owners keep the full surface | local 2026-07-20T20:53 |
| Webhooks (typed catalog, signed, logged) | local | `/p/webhooks` → URL + pick events → Create → mutate a record | the endpoint receives an HMAC-signed POST (verifiable with the once-shown secret); the per-endpoint delivery log shows the row; test-send button | local 2026-07-20T20:53 |
| Record subscriptions (@mentions notify watchers) | local | record header → Watch; Notes → type `@` | watchers get an outbox mail on notes/activities/updates (actor excluded); `@Name` autocompletes from the directory and auto-subscribes the mentioned account | local 2026-07-20T20:53 |
| Job seam (queue + recurring digest) | local | set `DIGEST_EVERY_MS` (+ optional `DIGEST_TO`) | the digest job writes per-object rollups into `app_state["digest:latest"]` on schedule; `/api/jobs` shows runs with status/retries | local 2026-07-20T20:53 |
| MCP server (AI assistant over the data model) | local | `claude mcp add my-app -- node scripts/mcp-server.mjs` (app running) | an assistant lists entities, reads schemas, queries live records and timelines through five read-only MCP tools | local 2026-07-20T20:53 |
| Theme editor (live, persisted) | local | `/p/theme` → pick a preset / brand color / corners / shell | edits apply INSTANTLY app-wide and survive reload (app_state); Export JSON emits the skin for the config | local 2026-07-20T20:53 |
| Feature flags (one flag: nav + page + API) | local | boot with `FEATURE_WEBHOOKS=0` | the nav entry disappears AND the API 404s — one env flag, all three enforcement points | local 2026-07-20T20:53 |
| Team-scoped objects (per-team visibility + roles) | local | `"teamScoped": true` on an object → sidebar team switcher | rows are stamped to the active team; other teams see an empty list and 404 by id; acting under a team you don't belong to is refused; roles resolve PER TEAM | local 2026-07-20T20:53 |
| Own-record grants (editOwn/deleteOwn) | local | grant `member: ["view","create","editOwn"]` | rows carry their creator; the creator edits their own rows, others 403; the record page renders read-only for foreign rows and editable for your own | local 2026-07-20T20:53 |
| Team activity log + invitation revocation | local | `/p/team` → Activity; remove a pending member | create/invite/join/role-change/remove/revoke are all logged with actors; revoking a pending invitation kills its mail token | local 2026-07-20T20:53 |
| Native warehouse spine (restart-survivable) | wf_native (Google Cloud connector) | set `WAREHOUSE=bigquery` + `NEXUS_API_KEY` + `WAREHOUSE_CREDENTIAL_ID` → use the app → restart it | every record/note/team/file action replays from the append-only events table with the SAME ids and timestamps — a restart loses nothing; without the env, the in-memory mock serves identically | local 2026-07-20T20:53 |
| Field-type depth (boolean/rating/dateTime/array/longText/json, colored options, unique, deactivation) | local | boot `journeys/fixtures/depth.config.json` → Vendors | colored option chips everywhere a select renders; boolean/rating edit inline; array adds free tags; unique fields 409 duplicates by name; `isActive:false` hides a field from every surface AND write-protects it while the data survives | local 2026-07-20T20:53 |
| Saved views (named, shareable, per-object) | local | Views menu → shape filters/layout → “Save current as view” | the view is server-persisted and listed for every workspace member; applying it restores layout AND filters; “All …” resets to the default state | local 2026-07-20T20:53 |
| Side peek (stacked record panel over the list) | local | click any row → panel opens over the list; open a related record inside it | the list stays visible behind; related records STACK (crumbs show the path); Escape steps back then closes; the root rides the URL (`?peek=`) so reload restores it; a promote button escapes to the full page | local 2026-07-20T20:53 |
| Record-to-record pagination (N of M through the open set) | local | open a peek from a list → header pager | "N of M" reflects the position in the exact set you opened from; prev/next step through it and wrap | local 2026-07-20T20:53 |
| Spreadsheet keyboard model (row → cell → edit, type-to-edit) | local | focus a table → ↑↓/jk · x select · Enter → cell · arrows · type | typing on a focused cell opens the editor SEEDED with the keystroke; Enter saves + steps down, Tab sideways; Escape climbs edit → cell → row → selection; Cmd/Ctrl+Enter peeks | local 2026-07-20T20:53 |
| Inline create inside the relation picker (no-match → create & attach) | local | record page → relation picker → type a name that doesn't exist | a "Create …" row appears; one click creates the record with just that title, attaches it, and opens it for field-by-field completion | local 2026-07-20T20:53 |
| Context actions in the command palette | local | ⌘K on a list vs on a record's full page | the palette leads with actions for what's on screen: New record / Open trash on lists; Add/remove favorite on records (with the panel open, ⌘K goes to the panel's actions page instead) | local 2026-07-20T20:53 |
| Trash (soft delete, restore, permanent destroy) | local | select rows → Delete → toolbar trash icon | deleted rows leave the list but sit in the Trash dialog; Restore brings one back intact; “Delete forever” removes it from the trash and the API 404s it | local 2026-07-20T20:53 |
| Unique collision restores the trashed twin (upsert semantic) | local | trash a record with a unique field → create a new one with the same value | the SAME record id returns (200, `_resurrected: true`) carrying the new data; a collision with a LIVE row still 400s | local 2026-07-20T20:53 |
| Merge duplicates (winner priority, relations re-pointed) | local | select 2–10 rows → Merge → pick the survivor | preview table shows the final value per field and WHERE inherited values come from; after merge one row remains, relations elsewhere point at it, losers sit in the trash | local 2026-07-20T20:53 |
| Favorites (personal pin shelf) | local | open a record → ★ Favorite | a Favorites section appears in the sidebar; its link deep-links back to the record; unpinning clears it | local 2026-07-20T20:53 |
| Destroy is a separate grant (delete ≠ destroy) | local | permissions grant `delete` but not `destroy` → open the trash as each role | the delete-holder sees Restore only; “Delete forever” renders solely for roles granted `destroy` (owners always); the API 403s the rest | local 2026-07-20T20:53 |
| Trash retention sweep (TRASH_RETENTION_DAYS) | local | set `TRASH_RETENTION_DAYS` → trash a row → wait a sweep | expired trash disappears on its own; the sweep shows up as a done `trash-sweep` job | local 2026-07-20T20:53 |
| Kanban per-column rollups (sum/avg/min/max) | local | deals Board → Rollup → “Sum of Amount” | every column head shows its rolled-up number (pipeline value per stage); the choice persists per object | local 2026-07-20T20:53 |
| Bulk edit (batched, live progress) | local | select rows → Edit → pick field + value → Update all | all selected rows take the value with a live N/M progress readout and cancel; empty value clears the field | local 2026-07-20T20:53 |
| CSV import wizard | local | any list → Import → paste CSV or pick a .csv → map columns → preview → run | column selects auto-map on name/label match; preview shows per-row verdicts with validator reasons; summary counts created/restored/skipped/failed; the list count rises; failed rows download as a CSV with a reason column; a unique value matching a trashed row restores it under its original id | local 2026-07-20T20:53 |
| API keys (role-scoped access) | local | /p/apikeys → name + role → Create → call the API with `x-api-key` | the full `nak_…` key renders exactly once; a viewer key reads rows but a PATCH with it 403s; revoking flips the row to revoked and the key answers 401; team-scoped objects refuse key callers | local 2026-07-20T20:53 |
| Multi-level sort (shift-click tie-breaker) | local | click one header, shift-click a second | rows sort by the first key with the second as tie-breaker; both levels persist in the saved state | local 2026-07-20T20:53 |
| Composite fields render (money/emails/phones/links/address/fullName) | local | `CONFIG_PATH=journeys/fixtures/depth.config.json npm run serve` → Vendors table + Sales reps board | money renders “€12,500” right-aligned; email/phone chips show first + “+N”; links anchor with the bare host (new tab); address one-lines “street, city”; a fullName primary titles the row link, record page and kanban card; a money rollup sums per column | local 2026-07-20T20:53 |
| Composite field editors (one-patch shaped saves) | local | open a vendor record → add an email, change the amount, set the city, change the last name | each edit saves as ONE patch (toast), survives reload, and the timeline logs readable old → new (“13750 EUR”, “…, Ghent”) | local 2026-07-20T20:53 |
| Composite validation (per-entry, field-named) | local | vendor record → type a bad email / bad URL / bad currency code into the editors | a visible inline error NAMES the field; the record keeps its old value; wrong-shaped API payloads 400 with the field named | local 2026-07-20T20:53 |
| Composite CSV flatten (shaped values in exports) | local | Vendors → select 2 rows → Export CSV | the downloaded file carries “12500 EUR”, “a; b” lists, the joined address/name — and a comma inside a URL stays one quoted column | local 2026-07-20T20:53 |
| Nav placement (config app.nav: top) | local | set `app.nav: "top"` in the config → reload (fixture: `journeys/fixtures/navtop.config.json`) | the nav renders as one horizontal top bar (brand, items with live counts, search/theme/sign-out on the right); no sidebar; clicking an item switches the list | local 2026-07-20T20:53 |
| Mobile nav drawer + full-screen peek | local | viewport ≤768px (side or top nav mode) → tap the burger | a left drawer lists objects/pages/favorites/team/sign-out; picking one navigates and closes it; an open record peek covers the full viewport; no horizontal page scroll at 390px | local 2026-07-20T20:53 |
| Possible duplicates panel (record page) | local | open a record that has a suspected twin | a "Possible duplicates" section lists each candidate WITH the matched rule ("Same name ignoring case…", "Same email"…) and announces "Review merge"; one click lands in the existing merge dialog preselected; Cancel keeps both records and leaves the pair selected | local 2026-07-20T20:53 |
| Duplicate sweep (find + review-merge) | local | any list → Find duplicates | a dialog groups suspected duplicates with member names + the rules that matched; per-group Review merge opens the merge dialog preselected; a clean object reports "No duplicates found."; nothing mutates without the merge confirmation | local 2026-07-20T20:53 |
| Relation identity (store ids, read labels) | local | depth fixture → open a vendor → pick a Sales rep → rename that rep | the picker lists fullName targets by joined name; the saved link is an ID (`_refs`); renaming the rep updates every inbound cell/section with NO sweep; the rep record shows the inverse-labeled “Vendors covered” section | local 2026-07-20T20:53 |
| Many-to-many relations (one committed diff on close) | local | vendor record → Certifications → toggle two options → Escape | toggles stay local while open; closing fires exactly ONE write with both ids; chips render both labels; reopening shows them checked; a chip’s × detaches just that link | local 2026-07-20T20:53 |
| Polymorphic relations (cross-type search, type labels) | local | vendor-note record → About → search “Atlas” | one search lists a vendor AND a rep with per-row type tags; picking each links the RIGHT identity (`_refs` carries object+id); the note lists on its target’s record under the poly field’s inverse label | local 2026-07-20T20:53 |
| Relation integrity (merge re-points ids; trash keeps, destroy severs) | local | merge two reps → trash/restore the winner → destroy a linked certification | after merge every inbound link points at the winner’s id; a trashed target keeps projecting its label and restore heals with zero writes; destroy removes the id from every pointing row | local 2026-07-20T20:53 |
| Composite-aware create dialog (shaped drafts + id-mode relations) | local | Sales reps → New → first/last + quota; Vendor notes → New → pick About | the dialog authors fullName + money as SHAPED values (code uppercased); the poly select is grouped by type and saves a typed ref that reads back as the target’s label | local 2026-07-20T20:53 |
| Relation identity replays (warehouse log, rename-safe) | mock | boot the depth fixture with the mock warehouse → label-writes + renames → restart | replayed state equals live state byte-for-byte: ids survive renames, labels re-project, a label written AFTER a rename stays dangling verbatim | local 2026-07-20T20:53 |
| Tasks page (buckets, filters, complete) | local | nav → Tasks → create tasks with due dates → complete one | tasks group into Overdue/Today/This week/Later buckets; completing one visibly moves it to Done (API stamps doneAt); the status filter narrows the visible rows | local 2026-07-20T20:53 |
| Tasks on records (link, complete, unlink) | local | open a record → Tasks card → add a task → complete it → unlink | the task appears in the record's Tasks card AND on the Tasks page with a chip back to the record; completing stamps "Task done: …" on the record timeline; unlinking removes it from the record but keeps the task | local 2026-07-20T20:53 |
| Task assignment mail | local | AUTH_MODE=accounts → create a task assigned to a signed-up user | the assignment mail ("Task assigned: …") lands in the dev outbox for the assignee — never for the actor | local 2026-07-20T20:53 |
| Panel search page (slash) | local | press `/` on any list → type a cross-object query | the panel opens on a search page with an always-focused input; results span objects with type labels; Enter pushes the hit onto the SAME panel (crumbs "Search › …") while the list behind stays untouched | local 2026-07-20T20:53 |
| Panel actions page (context actions) | local | open a record peek → the ⚡ header button (or Cmd/Ctrl+K while the panel is open) | an actions page stacks over the record listing the palette's context actions; running Favorite pins the record; "Search records" pushes the search page (3-deep crumbs); Escape walks the whole stack closed | local 2026-07-20T20:53 |
| Slash-key guard (grid + dialogs win) | local | focus a table cell, press `/`; open a dialog, press `/` | on a cell, `/` seeds the inline editor and no panel opens; with a dialog open, `/` types into the dialog; from ROW focus (navigation), `/` opens search | local 2026-07-20T20:53 |
| Runtime schema: add a field (colored select, everywhere on reload) | local | Schema page → Add field on an object → select type + colored options → Create | the commit reloads the app; the field appears on the schema page, as a table column, a filter chip, and a record-page editor; a saved value lands in the API and the timeline | local 2026-07-20T20:53 |
| Runtime schema: add an object (full CRUD + runtime relation) | local | Schema page → Add object → then Add field (relation) on it | nav grows the object; a row creates through its dialog; the runtime relation links by ID through the identity picker and projects the target's label; trash + restore round-trips | local 2026-07-20T20:53 |
| Runtime schema: retire + re-activate (data preserved) | local | Schema page → Retire on a field with data → Re-activate | the field leaves the table and record page while the API still returns its stored values and rejects writes (400); re-activation brings the values back intact | local 2026-07-20T20:53 |
| Runtime schema replays (deltas + data interleaved, restart byte-equal) | mock | mock warehouse → schema ops + dependent data writes → restart | after restart, the merged config AND every object's rows equal the pre-restart state byte-for-byte — deltas re-apply at their original position in the log | local 2026-07-20T20:53 |
| Runtime schema guard rails (every block names its reason) | local | Schema page → try a duplicate key, remove an in-use option, set unique over duplicates | each block surfaces a message NAMING the reason (the colliding key, the held option value, the duplicate values); type-change-with-data, key changes, and retiring the primary 400 with named reasons | local 2026-07-20T20:53 |
| UI gallery (catalog + live skin switching) | local | nav → Gallery → click a skin → reset | primitives, a live vendored subset, the full inventory (self-checking snapshot stamp), record-core minis and every field type render on ONE page; picking ember repaints it all instantly; reset restores the app skin; nothing persists (nx-skin-css untouched); the rest of the app is unaffected | local 2026-07-20T20:53 |
| Gallery record-core sandbox (local rows) | local | Gallery → Record core section → use the keyboard grid | the mini DataTable's full keyboard model works on LOCAL fictional rows (type-to-edit commits a visible value) with zero /api requests — the page works on an empty app | local 2026-07-20T20:53 |
| Rich-text editor (richText field) | local | Docs fixture → open a doc → type in a block → space + `/` → pick Heading 1 | the editor renders the seeded `Block[]`; `/` opens the slash menu; the paragraph becomes an `h1`; a "Saved" chip appears; the transform + text survive a reload | local 2026-07-20T20:53 |
| Rich-text editor drag-reorder | local | Docs fixture → open a doc → drag the third block's handle above the first | the block order changes (ed3 to the top) and the new order persists across a reload | local 2026-07-20T20:53 |
| Rich-text concurrent-poll safety | local | Docs fixture → open a doc → type into a block → an external writer overwrites the body + renames the record | the poll re-renders the header (renamed) but the in-progress editor edit is NOT clobbered by the polled server body | local 2026-07-20T20:53 |
| Rich-text table preview | local | Docs fixture → Docs list | the richText cell shows readable truncated prose (never `[object Object]` or raw JSON) | local 2026-07-20T20:53 |
| App shell + nav | local | open `/` → click each nav item | active nav item highlights; page title + row count change | local 2026-07-20T20:36 |
| Companies table | local | nav → Companies | 8 seeded rows render; header sort toggles order | local 2026-07-20T20:36 |
| Record page + inline edit | local | Companies → open “Brightline Analytics” → edit City → blur | field persists after reload; timeline gains an `updated` event; toast “Saved” | local 2026-07-20T20:36 |
| Notes | local | any record → Notes tab → type + Add | note appears in list; timeline gains a `note` event | local 2026-07-20T20:36 |
| Deals board (kanban) | local | nav → Deals (Board view) | 5 stage columns; changing a deal’s Stage moves its card to the target column | local 2026-07-20T20:36 |
| Create record | local | Companies → New company → name → Create | dialog closes; record page opens; count increments | local 2026-07-20T20:36 |
| Global search | local | topbar search → type “bright” → Enter | list filters to matching rows; filter chip shows the query | local 2026-07-20T20:36 |
| Theme toggle | local | topbar moon/sun | background/ink invert; choice survives reload | local 2026-07-20T20:36 |
| Data spine (app_state kv) | mock | POST /api/state {key,value} → GET /api/state | latest-per-key value returned (append-only history behind it) | local 2026-07-20T20:36 |
| Command palette (⌘K) | local | Ctrl/⌘-K → type a record name → Enter | palette lists live record hits across objects; selecting one opens its record page | local 2026-07-20T20:36 |
| Saved view (filter+view persist) | local | filter People to “maya” → visit Deals → return | the filter text AND its applied result survive navigation (per-object persistence) | local 2026-07-20T20:36 |
| Relation link cells | local | Deals table → click a Company cell | lands on Companies FILTERED to that company (1 row) | local 2026-07-20T20:36 |
| Bulk select · CSV export · reviewed delete | local | select rows → Export CSV / Delete | a .csv downloads; Delete opens a review dialog NAMING the records; confirm removes them and the count drops | local 2026-07-20T20:36 |
| Embedded agent chat dock | local | set chat.embedUrl in starter.config.json | floating chat button + iframe panel; unconfigured → nothing renders | local 2026-07-20T20:36 |
| Custom pages + kit demo | local | nav → Kit demo | the registered page renders; empty form submit shows a zod error; chart svg + sheet + accordion work live | local 2026-07-20T20:36 |
| Auth seam (login gate) | local | set AUTH_USERS + APP_SECRET → open the app | login card gates the shell; wrong password shows an error; right password enters; the API 401s without a session and unlocks with the cookie | local 2026-07-20T20:36 |
| Virtualized big lists | local | any object >80 rows | the DOM renders a WINDOW (row count < total) while scroll reaches every row; total count stays correct | local 2026-07-20T20:36 |
| Column visibility + sort persist | local | Columns menu → uncheck a field; sort a header; navigate away and back | the column disappears and STAYS hidden; the sort order survives navigation; re-checking restores | local 2026-07-20T20:36 |
| Date fields (calendar) | local | Deals table shows formatted dates → open a deal → Close date → pick a day | table cells render “14 Aug 2026”-style; the calendar pick saves, survives reload, and lands in the timeline | local 2026-07-20T20:36 |
| Select-field filter chips | local | Companies → Industry chip → check Software | list narrows to matching rows; the chip shows the active count; clear-all restores | local 2026-07-20T20:36 |
| Relation picker + related lists | local | open a company → related People/Deals panels; open a deal → Company picker | related panels list linked records (click opens them); the combobox sets the relation, saves, and the jump button lands filtered on the target | local 2026-07-20T20:36 |
| Building-blocks litmus (record-system class by config) | local | `CONFIG_PATH=journeys/fixtures/coverage.config.json npm run serve` | the SAME build assembles the hardest known topology (two-sided relations · staged pipeline · dates · scores) with zero code changes — a continuous TEST of block coverage, not a shipped template | local 2026-07-20T20:36 |
| User (assignee) fields | local | open a deal → Owner → pick from the directory | combobox over `users` from config; table cells render avatar + name; pick saves + persists | local 2026-07-20T20:36 |
| Multiselect (tags) fields | local | People table tag chips → Tags filter → record page toggle | chips in cells; contains-any filtering; toggling a tag saves, persists, and updates chips | local 2026-07-20T20:36 |
| Board group-by any field | local | any object w/ a select field → Board; deals board → “by …” picker → Owner | companies (no stageField) still gets a Board via Industry; deals board regroups into user columns; the choice persists per object | local 2026-07-20T20:36 |
| Attachments (files on records) | local | record → Files tab → Upload file | file lists with name + size + date; Download returns the exact bytes; timeline gains an “Attached …” event | local 2026-07-20T20:36 |
| Activity composer (call/email/meeting) | local | record → Timeline tab → pick kind → type → Log | activity appears at the top of the timeline with its kind icon; survives reload | local 2026-07-20T20:36 |
| AI-enrichment seam (field.primitive) | mock | company record → sparkle next to About | the field fills with a labeled (mock) value; timeline logs “Enriched … via Company research (mock)”; swap the /enrich mock in server/server.mjs for a real task/workflow call | local 2026-07-20T20:36 |
| Field-type validation (config-implied) | local | open a person → set Email to “not-an-email” → blur | toast carries the server’s message (“… valid email address”); the field reverts; the API 400s | local 2026-07-20T20:36 |
| Demo-data badge | local | boot with seeded config → look at the sidebar footer | a “Demo data” chip is visible whenever seeded fictional rows exist; delete the seed and it disappears | local 2026-07-20T20:36 |
| Image downscale on upload | local | record → Files → upload a large image | the stored file’s pixel width is capped at 1600 (verified by reading the PNG header of the downloaded bytes) | local 2026-07-20T20:36 |
| Chart view (group + measure) | local | any object w/ a select/user field → Chart; deals → “Σ Amount” | one bar per group option with value labels; measure switches Count ↔ field sums (deals New = 53,000); group/measure choices persist per object | local 2026-07-20T20:36 |
| Live sync (cross-viewer updates) | local | open a table; edit the same object from another tab/agent | the change appears in the open view within ~5s, no reload (per-object rev poll; pauses when the tab is hidden) | local 2026-07-20T20:36 |
| Generator CLI (object scaffolding) | local | `npm run generate object Gadget -- --fields "name:text:primary,status:select:A\|B"` | the config gains the object; the app boots with its table/board + seeded rows; `generate page`/`journey` scaffold from editable templates | local 2026-07-20T20:36 |
| Skin system (org brand as data) | local | set `theme.skinPreset` (or an inline `theme.skin` object) → reload | the WHOLE app re-brands from one small JSON: accent ramp, dark chrome shell, radius personality (vendored shadcn kit included), fonts, labels, semantic palette, logo mark/wordmark; dark mode derives from the same brand | local 2026-07-20T20:36 |
| Accounts (signup + email verification) | local | `AUTH_MODE=accounts` + `APP_SECRET` → Create an account → follow the mail link | signup enters the app; the verification mail lands in the dev outbox (`/api/outbox`); its link flips the account to verified | local 2026-07-20T20:36 |
| Password reset (anti-enumeration) + delete-by-confirmation | local | Forgot password? → mail link → new password; account deletion asks for a mailed confirmation | known and unknown addresses answer identically (decoy mail keeps timing parity); reset kills the old password AND old sessions; deletion only completes from the mailed token | local 2026-07-20T20:36 |
| Teams (invitations, join code, roles) | local | `/p/team` → create → invite by mail / share the join code | pending → active membership states are visible; the invite mail's link activates; duplicate invites 409 explicitly; the last owner can't be demoted | local 2026-07-20T20:36 |
| Permissions (config-declared, role-enforced) | local | give an object a `permissions` block → sign in as a plain member | create button and inline editors disappear for uncovered actions AND the API 403s them; owners keep the full surface | local 2026-07-20T20:36 |
| Webhooks (typed catalog, signed, logged) | local | `/p/webhooks` → URL + pick events → Create → mutate a record | the endpoint receives an HMAC-signed POST (verifiable with the once-shown secret); the per-endpoint delivery log shows the row; test-send button | local 2026-07-20T20:36 |
| Record subscriptions (@mentions notify watchers) | local | record header → Watch; Notes → type `@` | watchers get an outbox mail on notes/activities/updates (actor excluded); `@Name` autocompletes from the directory and auto-subscribes the mentioned account | local 2026-07-20T20:36 |
| Job seam (queue + recurring digest) | local | set `DIGEST_EVERY_MS` (+ optional `DIGEST_TO`) | the digest job writes per-object rollups into `app_state["digest:latest"]` on schedule; `/api/jobs` shows runs with status/retries | local 2026-07-20T20:36 |
| MCP server (AI assistant over the data model) | local | `claude mcp add my-app -- node scripts/mcp-server.mjs` (app running) | an assistant lists entities, reads schemas, queries live records and timelines through five read-only MCP tools | local 2026-07-20T20:36 |
| Theme editor (live, persisted) | local | `/p/theme` → pick a preset / brand color / corners / shell | edits apply INSTANTLY app-wide and survive reload (app_state); Export JSON emits the skin for the config | local 2026-07-20T20:36 |
| Feature flags (one flag: nav + page + API) | local | boot with `FEATURE_WEBHOOKS=0` | the nav entry disappears AND the API 404s — one env flag, all three enforcement points | local 2026-07-20T20:36 |
| Team-scoped objects (per-team visibility + roles) | local | `"teamScoped": true` on an object → sidebar team switcher | rows are stamped to the active team; other teams see an empty list and 404 by id; acting under a team you don't belong to is refused; roles resolve PER TEAM | local 2026-07-20T20:36 |
| Own-record grants (editOwn/deleteOwn) | local | grant `member: ["view","create","editOwn"]` | rows carry their creator; the creator edits their own rows, others 403; the record page renders read-only for foreign rows and editable for your own | local 2026-07-20T20:36 |
| Team activity log + invitation revocation | local | `/p/team` → Activity; remove a pending member | create/invite/join/role-change/remove/revoke are all logged with actors; revoking a pending invitation kills its mail token | local 2026-07-20T20:36 |
| Native warehouse spine (restart-survivable) | wf_native (Google Cloud connector) | set `WAREHOUSE=bigquery` + `NEXUS_API_KEY` + `WAREHOUSE_CREDENTIAL_ID` → use the app → restart it | every record/note/team/file action replays from the append-only events table with the SAME ids and timestamps — a restart loses nothing; without the env, the in-memory mock serves identically | local 2026-07-20T20:36 |
| Field-type depth (boolean/rating/dateTime/array/longText/json, colored options, unique, deactivation) | local | boot `journeys/fixtures/depth.config.json` → Vendors | colored option chips everywhere a select renders; boolean/rating edit inline; array adds free tags; unique fields 409 duplicates by name; `isActive:false` hides a field from every surface AND write-protects it while the data survives | local 2026-07-20T20:36 |
| Saved views (named, shareable, per-object) | local | Views menu → shape filters/layout → “Save current as view” | the view is server-persisted and listed for every workspace member; applying it restores layout AND filters; “All …” resets to the default state | local 2026-07-20T20:36 |
| Side peek (stacked record panel over the list) | local | click any row → panel opens over the list; open a related record inside it | the list stays visible behind; related records STACK (crumbs show the path); Escape steps back then closes; the root rides the URL (`?peek=`) so reload restores it; a promote button escapes to the full page | local 2026-07-20T20:36 |
| Record-to-record pagination (N of M through the open set) | local | open a peek from a list → header pager | "N of M" reflects the position in the exact set you opened from; prev/next step through it and wrap | local 2026-07-20T20:36 |
| Spreadsheet keyboard model (row → cell → edit, type-to-edit) | local | focus a table → ↑↓/jk · x select · Enter → cell · arrows · type | typing on a focused cell opens the editor SEEDED with the keystroke; Enter saves + steps down, Tab sideways; Escape climbs edit → cell → row → selection; Cmd/Ctrl+Enter peeks | local 2026-07-20T20:36 |
| Inline create inside the relation picker (no-match → create & attach) | local | record page → relation picker → type a name that doesn't exist | a "Create …" row appears; one click creates the record with just that title, attaches it, and opens it for field-by-field completion | local 2026-07-20T20:36 |
| Context actions in the command palette | local | ⌘K on a list vs on a record's full page | the palette leads with actions for what's on screen: New record / Open trash on lists; Add/remove favorite on records (with the panel open, ⌘K goes to the panel's actions page instead) | local 2026-07-20T20:36 |
| Trash (soft delete, restore, permanent destroy) | local | select rows → Delete → toolbar trash icon | deleted rows leave the list but sit in the Trash dialog; Restore brings one back intact; “Delete forever” removes it from the trash and the API 404s it | local 2026-07-20T20:36 |
| Unique collision restores the trashed twin (upsert semantic) | local | trash a record with a unique field → create a new one with the same value | the SAME record id returns (200, `_resurrected: true`) carrying the new data; a collision with a LIVE row still 400s | local 2026-07-20T20:36 |
| Merge duplicates (winner priority, relations re-pointed) | local | select 2–10 rows → Merge → pick the survivor | preview table shows the final value per field and WHERE inherited values come from; after merge one row remains, relations elsewhere point at it, losers sit in the trash | local 2026-07-20T20:36 |
| Favorites (personal pin shelf) | local | open a record → ★ Favorite | a Favorites section appears in the sidebar; its link deep-links back to the record; unpinning clears it | local 2026-07-20T20:36 |
| Destroy is a separate grant (delete ≠ destroy) | local | permissions grant `delete` but not `destroy` → open the trash as each role | the delete-holder sees Restore only; “Delete forever” renders solely for roles granted `destroy` (owners always); the API 403s the rest | local 2026-07-20T20:36 |
| Trash retention sweep (TRASH_RETENTION_DAYS) | local | set `TRASH_RETENTION_DAYS` → trash a row → wait a sweep | expired trash disappears on its own; the sweep shows up as a done `trash-sweep` job | local 2026-07-20T20:36 |
| Kanban per-column rollups (sum/avg/min/max) | local | deals Board → Rollup → “Sum of Amount” | every column head shows its rolled-up number (pipeline value per stage); the choice persists per object | local 2026-07-20T20:36 |
| Bulk edit (batched, live progress) | local | select rows → Edit → pick field + value → Update all | all selected rows take the value with a live N/M progress readout and cancel; empty value clears the field | local 2026-07-20T20:36 |
| CSV import wizard | local | any list → Import → paste CSV or pick a .csv → map columns → preview → run | column selects auto-map on name/label match; preview shows per-row verdicts with validator reasons; summary counts created/restored/skipped/failed; the list count rises; failed rows download as a CSV with a reason column; a unique value matching a trashed row restores it under its original id | local 2026-07-20T20:36 |
| API keys (role-scoped access) | local | /p/apikeys → name + role → Create → call the API with `x-api-key` | the full `nak_…` key renders exactly once; a viewer key reads rows but a PATCH with it 403s; revoking flips the row to revoked and the key answers 401; team-scoped objects refuse key callers | local 2026-07-20T20:36 |
| Multi-level sort (shift-click tie-breaker) | local | click one header, shift-click a second | rows sort by the first key with the second as tie-breaker; both levels persist in the saved state | local 2026-07-20T20:36 |
| Composite fields render (money/emails/phones/links/address/fullName) | local | `CONFIG_PATH=journeys/fixtures/depth.config.json npm run serve` → Vendors table + Sales reps board | money renders “€12,500” right-aligned; email/phone chips show first + “+N”; links anchor with the bare host (new tab); address one-lines “street, city”; a fullName primary titles the row link, record page and kanban card; a money rollup sums per column | local 2026-07-20T20:36 |
| Composite field editors (one-patch shaped saves) | local | open a vendor record → add an email, change the amount, set the city, change the last name | each edit saves as ONE patch (toast), survives reload, and the timeline logs readable old → new (“13750 EUR”, “…, Ghent”) | local 2026-07-20T20:36 |
| Composite validation (per-entry, field-named) | local | vendor record → type a bad email / bad URL / bad currency code into the editors | a visible inline error NAMES the field; the record keeps its old value; wrong-shaped API payloads 400 with the field named | local 2026-07-20T20:36 |
| Composite CSV flatten (shaped values in exports) | local | Vendors → select 2 rows → Export CSV | the downloaded file carries “12500 EUR”, “a; b” lists, the joined address/name — and a comma inside a URL stays one quoted column | local 2026-07-20T20:36 |
| Nav placement (config app.nav: top) | local | set `app.nav: "top"` in the config → reload (fixture: `journeys/fixtures/navtop.config.json`) | the nav renders as one horizontal top bar (brand, items with live counts, search/theme/sign-out on the right); no sidebar; clicking an item switches the list | local 2026-07-20T20:36 |
| Mobile nav drawer + full-screen peek | local | viewport ≤768px (side or top nav mode) → tap the burger | a left drawer lists objects/pages/favorites/team/sign-out; picking one navigates and closes it; an open record peek covers the full viewport; no horizontal page scroll at 390px | local 2026-07-20T20:36 |
| Possible duplicates panel (record page) | local | open a record that has a suspected twin | a "Possible duplicates" section lists each candidate WITH the matched rule ("Same name ignoring case…", "Same email"…) and announces "Review merge"; one click lands in the existing merge dialog preselected; Cancel keeps both records and leaves the pair selected | local 2026-07-20T20:36 |
| Duplicate sweep (find + review-merge) | local | any list → Find duplicates | a dialog groups suspected duplicates with member names + the rules that matched; per-group Review merge opens the merge dialog preselected; a clean object reports "No duplicates found."; nothing mutates without the merge confirmation | local 2026-07-20T20:36 |
| Relation identity (store ids, read labels) | local | depth fixture → open a vendor → pick a Sales rep → rename that rep | the picker lists fullName targets by joined name; the saved link is an ID (`_refs`); renaming the rep updates every inbound cell/section with NO sweep; the rep record shows the inverse-labeled “Vendors covered” section | local 2026-07-20T20:36 |
| Many-to-many relations (one committed diff on close) | local | vendor record → Certifications → toggle two options → Escape | toggles stay local while open; closing fires exactly ONE write with both ids; chips render both labels; reopening shows them checked; a chip’s × detaches just that link | local 2026-07-20T20:36 |
| Polymorphic relations (cross-type search, type labels) | local | vendor-note record → About → search “Atlas” | one search lists a vendor AND a rep with per-row type tags; picking each links the RIGHT identity (`_refs` carries object+id); the note lists on its target’s record under the poly field’s inverse label | local 2026-07-20T20:36 |
| Relation integrity (merge re-points ids; trash keeps, destroy severs) | local | merge two reps → trash/restore the winner → destroy a linked certification | after merge every inbound link points at the winner’s id; a trashed target keeps projecting its label and restore heals with zero writes; destroy removes the id from every pointing row | local 2026-07-20T20:36 |
| Composite-aware create dialog (shaped drafts + id-mode relations) | local | Sales reps → New → first/last + quota; Vendor notes → New → pick About | the dialog authors fullName + money as SHAPED values (code uppercased); the poly select is grouped by type and saves a typed ref that reads back as the target’s label | local 2026-07-20T20:36 |
| Relation identity replays (warehouse log, rename-safe) | mock | boot the depth fixture with the mock warehouse → label-writes + renames → restart | replayed state equals live state byte-for-byte: ids survive renames, labels re-project, a label written AFTER a rename stays dangling verbatim | local 2026-07-20T20:36 |
| Tasks page (buckets, filters, complete) | local | nav → Tasks → create tasks with due dates → complete one | tasks group into Overdue/Today/This week/Later buckets; completing one visibly moves it to Done (API stamps doneAt); the status filter narrows the visible rows | local 2026-07-20T20:36 |
| Tasks on records (link, complete, unlink) | local | open a record → Tasks card → add a task → complete it → unlink | the task appears in the record's Tasks card AND on the Tasks page with a chip back to the record; completing stamps "Task done: …" on the record timeline; unlinking removes it from the record but keeps the task | local 2026-07-20T20:36 |
| Task assignment mail | local | AUTH_MODE=accounts → create a task assigned to a signed-up user | the assignment mail ("Task assigned: …") lands in the dev outbox for the assignee — never for the actor | local 2026-07-20T20:36 |
| Panel search page (slash) | local | press `/` on any list → type a cross-object query | the panel opens on a search page with an always-focused input; results span objects with type labels; Enter pushes the hit onto the SAME panel (crumbs "Search › …") while the list behind stays untouched | local 2026-07-20T20:36 |
| Panel actions page (context actions) | local | open a record peek → the ⚡ header button (or Cmd/Ctrl+K while the panel is open) | an actions page stacks over the record listing the palette's context actions; running Favorite pins the record; "Search records" pushes the search page (3-deep crumbs); Escape walks the whole stack closed | local 2026-07-20T20:36 |
| Slash-key guard (grid + dialogs win) | local | focus a table cell, press `/`; open a dialog, press `/` | on a cell, `/` seeds the inline editor and no panel opens; with a dialog open, `/` types into the dialog; from ROW focus (navigation), `/` opens search | local 2026-07-20T20:36 |
| Runtime schema: add a field (colored select, everywhere on reload) | local | Schema page → Add field on an object → select type + colored options → Create | the commit reloads the app; the field appears on the schema page, as a table column, a filter chip, and a record-page editor; a saved value lands in the API and the timeline | local 2026-07-20T20:36 |
| Runtime schema: add an object (full CRUD + runtime relation) | local | Schema page → Add object → then Add field (relation) on it | nav grows the object; a row creates through its dialog; the runtime relation links by ID through the identity picker and projects the target's label; trash + restore round-trips | local 2026-07-20T20:36 |
| Runtime schema: retire + re-activate (data preserved) | local | Schema page → Retire on a field with data → Re-activate | the field leaves the table and record page while the API still returns its stored values and rejects writes (400); re-activation brings the values back intact | local 2026-07-20T20:36 |
| Runtime schema replays (deltas + data interleaved, restart byte-equal) | mock | mock warehouse → schema ops + dependent data writes → restart | after restart, the merged config AND every object's rows equal the pre-restart state byte-for-byte — deltas re-apply at their original position in the log | local 2026-07-20T20:36 |
| Runtime schema guard rails (every block names its reason) | local | Schema page → try a duplicate key, remove an in-use option, set unique over duplicates | each block surfaces a message NAMING the reason (the colliding key, the held option value, the duplicate values); type-change-with-data, key changes, and retiring the primary 400 with named reasons | local 2026-07-20T20:36 |
| UI gallery (catalog + live skin switching) | local | nav → Gallery → click a skin → reset | primitives, a live vendored subset, the full inventory (self-checking snapshot stamp), record-core minis and every field type render on ONE page; picking ember repaints it all instantly; reset restores the app skin; nothing persists (nx-skin-css untouched); the rest of the app is unaffected | local 2026-07-20T20:36 |
| Gallery record-core sandbox (local rows) | local | Gallery → Record core section → use the keyboard grid | the mini DataTable's full keyboard model works on LOCAL fictional rows (type-to-edit commits a visible value) with zero /api requests — the page works on an empty app | local 2026-07-20T20:36 |
| Wizard — config-driven multi-step block | local | Gallery → Wizard section → Open wizard → guided → step through kind/ask/notes/tags/sources → Generate draft | picking a select option auto-advances; Next disables on an empty required step; the review screen lists every answered step (incl. chips and the source URL); completing fires a toast with the collected answer and closes the modal | local 2026-07-20T20:36 |
