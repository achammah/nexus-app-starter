# Feature/test manifest

One row per feature, written the wave it ships. Primitive = the platform primitive id behind it (`wf_…`/`task_…`/`agent_…`), or `mock` (labeled in the UI), or `local` for pure-UI features with no platform twin. A proof must be VISIBLE ("the API returns 200" is not visible). `Last verified` is written by `npm run journeys`, never by hand.

| Feature | Primitive | How to exercise (exact click path) | VISIBLE outcome that proves it | Last verified (local / hosted) |
|---|---|---|---|---|
| App shell + nav | local | open `/` → click each nav item | active nav item highlights; page title + row count change | local 2026-07-21T21:12 |
| Companies table | local | nav → Companies | 8 seeded rows render; header sort toggles order | local 2026-07-21T21:12 |
| Record page + inline edit | local | Companies → open “Brightline Analytics” → edit City → blur | field persists after reload; timeline gains an `updated` event; toast “Saved” | local 2026-07-21T21:12 |
| Notes | local | any record → Notes tab → type + Add | note appears in list; timeline gains a `note` event | local 2026-07-21T21:12 |
| Deals board (kanban) | local | nav → Deals (Board view) | 5 stage columns; changing a deal’s Stage moves its card to the target column | local 2026-07-21T21:12 |
| Create record | local | Companies → New company → name → Create | dialog closes; record page opens; count increments | local 2026-07-21T21:12 |
| Global search | local | topbar search → type “bright” → Enter | list filters to matching rows; filter chip shows the query | local 2026-07-21T21:12 |
| Theme toggle | local | topbar moon/sun | background/ink invert; choice survives reload | local 2026-07-21T21:12 |
| Data spine (app_state kv) | mock | POST /api/state {key,value} → GET /api/state | latest-per-key value returned (append-only history behind it) | local 2026-07-21T21:12 |
| Command palette (⌘K) | local | Ctrl/⌘-K → type a record name → Enter | palette lists live record hits across objects; selecting one opens its record page | local 2026-07-21T21:12 |
| Saved view (filter+view persist) | local | filter People to “maya” → visit Deals → return | the filter text AND its applied result survive navigation (per-object persistence) | local 2026-07-21T21:12 |
| Relation link cells | local | Deals table → click a Company cell | lands on Companies FILTERED to that company (1 row) | local 2026-07-21T21:12 |
| Bulk select · CSV export · reviewed delete | local | select rows → Export CSV / Delete | a .csv downloads; Delete opens a review dialog NAMING the records; confirm removes them and the count drops | local 2026-07-21T21:12 |
| Embedded agent chat dock | local | set chat.embedUrl in starter.config.json | floating chat button + iframe panel; unconfigured → nothing renders | local 2026-07-21T21:12 |
| Custom pages + kit demo | local | nav → Kit demo | the registered page renders; empty form submit shows a zod error; chart svg + sheet + accordion work live | local 2026-07-21T21:12 |
| Auth seam (login gate) | local | set AUTH_USERS + APP_SECRET → open the app | login card gates the shell; wrong password shows an error; right password enters; the API 401s without a session and unlocks with the cookie | local 2026-07-21T21:12 |
| Virtualized big lists | local | any object >80 rows | the DOM renders a WINDOW (row count < total) while scroll reaches every row; total count stays correct | local 2026-07-21T21:12 |
| Column visibility + sort persist | local | Columns menu → uncheck a field; sort a header; navigate away and back | the column disappears and STAYS hidden; the sort order survives navigation; re-checking restores | local 2026-07-21T21:12 |
| Date fields (calendar) | local | Deals table shows formatted dates → open a deal → Close date → pick a day | table cells render “14 Aug 2026”-style; the calendar pick saves, survives reload, and lands in the timeline | local 2026-07-21T21:12 |
| Select-field filter chips | local | Companies → Industry chip → check Software | list narrows to matching rows; the chip shows the active count; clear-all restores | local 2026-07-21T21:12 |
| Relation picker + related lists | local | open a company → related People/Deals panels; open a deal → Company picker | related panels list linked records (click opens them); the combobox sets the relation, saves, and the jump button lands filtered on the target | local 2026-07-21T21:12 |
| Building-blocks litmus (record-system class by config) | local | `CONFIG_PATH=journeys/fixtures/coverage.config.json npm run serve` | the SAME build assembles the hardest known topology (two-sided relations · staged pipeline · dates · scores) with zero code changes — a continuous TEST of block coverage, not a shipped template | local 2026-07-21T21:12 |
| User (assignee) fields | local | open a deal → Owner → pick from the directory | combobox over `users` from config; table cells render avatar + name; pick saves + persists | local 2026-07-21T21:12 |
| Multiselect (tags) fields | local | People table tag chips → Tags filter → record page toggle | chips in cells; contains-any filtering; toggling a tag saves, persists, and updates chips | local 2026-07-21T21:12 |
| Board group-by any field | local | any object w/ a select field → Board; deals board → “by …” picker → Owner | companies (no stageField) still gets a Board via Industry; deals board regroups into user columns; the choice persists per object | local 2026-07-21T21:12 |
| Attachments (files on records) | local | record → Files tab → Upload file | file lists with name + size + date; Download returns the exact bytes; timeline gains an “Attached …” event | local 2026-07-21T21:12 |
| Activity composer (call/email/meeting) | local | record → Timeline tab → pick kind → type → Log | activity appears at the top of the timeline with its kind icon; survives reload | local 2026-07-21T21:12 |
| AI-enrichment seam (field.primitive) | mock | company record → sparkle next to About | the field fills with a labeled (mock) value; timeline logs “Enriched … via Company research (mock)”; swap the /enrich mock in server/server.mjs for a real task/workflow call | local 2026-07-21T21:12 |
| Field-type validation (config-implied) | local | open a person → set Email to “not-an-email” → blur | toast carries the server’s message (“… valid email address”); the field reverts; the API 400s | local 2026-07-21T21:12 |
| Demo-data badge | local | boot with seeded config → look at the sidebar footer | a “Demo data” chip is visible whenever seeded fictional rows exist; delete the seed and it disappears | local 2026-07-21T21:12 |
| Image downscale on upload | local | record → Files → upload a large image | the stored file’s pixel width is capped at 1600 (verified by reading the PNG header of the downloaded bytes) | local 2026-07-21T21:12 |
| Chart view (group + measure) | local | any object w/ a select/user field → Chart; deals → “Σ Amount” | one bar per group option with value labels; measure switches Count ↔ field sums (deals New = 53,000); group/measure choices persist per object | local 2026-07-21T21:12 |
| Live sync (cross-viewer updates) | local | open a table; edit the same object from another tab/agent | the change appears in the open view within ~5s, no reload (per-object rev poll; pauses when the tab is hidden) | local 2026-07-21T21:12 |
| Generator CLI (object scaffolding) | local | `npm run generate object Gadget -- --fields "name:text:primary,status:select:A\|B"` | the config gains the object; the app boots with its table/board + seeded rows; `generate page`/`journey` scaffold from editable templates | local 2026-07-21T21:12 |
| Skin system (org brand as data) | local | set `theme.skinPreset` (or an inline `theme.skin` object) → reload | the WHOLE app re-brands from one small JSON: accent ramp, dark chrome shell, radius personality (vendored shadcn kit included), fonts, labels, semantic palette, logo mark/wordmark; dark mode derives from the same brand | local 2026-07-21T21:12 |
| Accounts (signup + email verification) | local | `AUTH_MODE=accounts` + `APP_SECRET` → Create an account → follow the mail link | signup enters the app; the verification mail lands in the dev outbox (`/api/outbox`); its link flips the account to verified | local 2026-07-21T21:12 |
| Password reset (anti-enumeration) + delete-by-confirmation | local | Forgot password? → mail link → new password; account deletion asks for a mailed confirmation | known and unknown addresses answer identically (decoy mail keeps timing parity); reset kills the old password AND old sessions; deletion only completes from the mailed token | local 2026-07-21T21:12 |
| Teams (invitations, join code, roles) | local | `/p/team` → create → invite by mail / share the join code | pending → active membership states are visible; the invite mail's link activates; duplicate invites 409 explicitly; the last owner can't be demoted | local 2026-07-21T21:12 |
| Permissions (config-declared, role-enforced) | local | give an object a `permissions` block → sign in as a plain member | create button and inline editors disappear for uncovered actions AND the API 403s them; owners keep the full surface | local 2026-07-21T21:12 |
| Webhooks (typed catalog, signed, logged) | local | `/p/webhooks` → URL + pick events → Create → mutate a record | the endpoint receives an HMAC-signed POST (verifiable with the once-shown secret); the per-endpoint delivery log shows the row; test-send button | local 2026-07-21T21:12 |
| Record subscriptions (@mentions notify watchers) | local | record header → Watch; Notes → type `@` | watchers get an outbox mail on notes/activities/updates (actor excluded); `@Name` autocompletes from the directory and auto-subscribes the mentioned account | local 2026-07-21T21:12 |
| Job seam (queue + recurring digest) | local | set `DIGEST_EVERY_MS` (+ optional `DIGEST_TO`) | the digest job writes per-object rollups into `app_state["digest:latest"]` on schedule; `/api/jobs` shows runs with status/retries | local 2026-07-21T21:12 |
| MCP server (AI assistant over the data model) | local | `claude mcp add my-app -- node scripts/mcp-server.mjs` (app running) | an assistant lists entities, reads schemas, queries live records and timelines through five read-only MCP tools | local 2026-07-21T21:12 |
| Theme editor (live, persisted) | local | `/p/theme` → pick a preset / brand color / corners / shell | edits apply INSTANTLY app-wide and survive reload (app_state); Export JSON emits the skin for the config | local 2026-07-21T21:12 |
| Feature flags (one flag: nav + page + API) | local | boot with `FEATURE_WEBHOOKS=0` | the nav entry disappears AND the API 404s — one env flag, all three enforcement points | local 2026-07-21T21:12 |
| Team-scoped objects (per-team visibility + roles) | local | `"teamScoped": true` on an object → sidebar team switcher | rows are stamped to the active team; other teams see an empty list and 404 by id; acting under a team you don't belong to is refused; roles resolve PER TEAM | local 2026-07-21T21:12 |
| Own-record grants (editOwn/deleteOwn) | local | grant `member: ["view","create","editOwn"]` | rows carry their creator; the creator edits their own rows, others 403; the record page renders read-only for foreign rows and editable for your own | local 2026-07-21T21:12 |
| Team activity log + invitation revocation | local | `/p/team` → Activity; remove a pending member | create/invite/join/role-change/remove/revoke are all logged with actors; revoking a pending invitation kills its mail token | local 2026-07-21T21:12 |
| Native warehouse spine (restart-survivable) | wf_native (Google Cloud connector) | set `WAREHOUSE=bigquery` + `NEXUS_API_KEY` + `WAREHOUSE_CREDENTIAL_ID` → use the app → restart it | every record/note/team/file action replays from the append-only events table with the SAME ids and timestamps — a restart loses nothing; without the env, the in-memory mock serves identically | local 2026-07-21T21:12 |
| Field-type depth (boolean/rating/dateTime/array/longText/json, colored options, unique, deactivation) | local | boot `journeys/fixtures/depth.config.json` → Vendors | colored option chips everywhere a select renders; boolean/rating edit inline; array adds free tags; unique fields 409 duplicates by name; `isActive:false` hides a field from every surface AND write-protects it while the data survives | local 2026-07-21T21:12 |
| Saved views (named, shareable, per-object) | local | Views menu → shape filters/layout → “Save current as view” | the view is server-persisted and listed for every workspace member; applying it restores layout AND filters; “All …” resets to the default state | local 2026-07-21T21:12 |
| Side peek (stacked record panel over the list) | local | click any row → panel opens over the list; open a related record inside it | the list stays visible behind; related records STACK (crumbs show the path); Escape steps back then closes; the root rides the URL (`?peek=`) so reload restores it; a promote button escapes to the full page | local 2026-07-21T21:12 |
| Record-to-record pagination (N of M through the open set) | local | open a peek from a list → header pager | "N of M" reflects the position in the exact set you opened from; prev/next step through it and wrap | local 2026-07-21T21:12 |
| Spreadsheet keyboard model (row → cell → edit, type-to-edit) | local | focus a table → ↑↓/jk · x select · Enter → cell · arrows · type | typing on a focused cell opens the editor SEEDED with the keystroke; Enter saves + steps down, Tab sideways; Escape climbs edit → cell → row → selection; Cmd/Ctrl+Enter peeks | local 2026-07-21T21:12 |
| Inline create inside the relation picker (no-match → create & attach) | local | record page → relation picker → type a name that doesn't exist | a "Create …" row appears; one click creates the record with just that title, attaches it, and opens it for field-by-field completion | local 2026-07-21T21:12 |
| Context actions in the command palette | local | ⌘K on a list vs on a record's full page | the palette leads with actions for what's on screen: New record / Open trash on lists; Add/remove favorite on records (with the panel open, ⌘K goes to the panel's actions page instead) | local 2026-07-21T21:12 |
| Trash (soft delete, restore, permanent destroy) | local | select rows → Delete → toolbar trash icon | deleted rows leave the list but sit in the Trash dialog; Restore brings one back intact; “Delete forever” removes it from the trash and the API 404s it | local 2026-07-21T21:12 |
| Unique collision restores the trashed twin (upsert semantic) | local | trash a record with a unique field → create a new one with the same value | the SAME record id returns (200, `_resurrected: true`) carrying the new data; a collision with a LIVE row still 400s | local 2026-07-21T21:12 |
| Merge duplicates (winner priority, relations re-pointed) | local | select 2–10 rows → Merge → pick the survivor | preview table shows the final value per field and WHERE inherited values come from; after merge one row remains, relations elsewhere point at it, losers sit in the trash | local 2026-07-21T21:12 |
| Favorites (personal pin shelf) | local | open a record → ★ Favorite | a Favorites section appears in the sidebar; its link deep-links back to the record; unpinning clears it | local 2026-07-21T21:12 |
| Destroy is a separate grant (delete ≠ destroy) | local | permissions grant `delete` but not `destroy` → open the trash as each role | the delete-holder sees Restore only; “Delete forever” renders solely for roles granted `destroy` (owners always); the API 403s the rest | local 2026-07-21T21:12 |
| Trash retention sweep (TRASH_RETENTION_DAYS) | local | set `TRASH_RETENTION_DAYS` → trash a row → wait a sweep | expired trash disappears on its own; the sweep shows up as a done `trash-sweep` job | local 2026-07-21T21:12 |
| Kanban per-column rollups (sum/avg/min/max) | local | deals Board → Rollup → “Sum of Amount” | every column head shows its rolled-up number (pipeline value per stage); the choice persists per object | local 2026-07-21T21:12 |
| Bulk edit (batched, live progress) | local | select rows → Edit → pick field + value → Update all | all selected rows take the value with a live N/M progress readout and cancel; empty value clears the field | local 2026-07-21T21:12 |
| CSV import wizard | local | any list → Import → paste CSV or pick a .csv → map columns → preview → run | column selects auto-map on name/label match; preview shows per-row verdicts with validator reasons; summary counts created/restored/skipped/failed; the list count rises; failed rows download as a CSV with a reason column; a unique value matching a trashed row restores it under its original id | local 2026-07-21T21:12 |
| API keys (role-scoped access) | local | /p/apikeys → name + role → Create → call the API with `x-api-key` | the full `nak_…` key renders exactly once; a viewer key reads rows but a PATCH with it 403s; revoking flips the row to revoked and the key answers 401; team-scoped objects refuse key callers | local 2026-07-21T21:12 |
| Multi-level sort (shift-click tie-breaker) | local | click one header, shift-click a second | rows sort by the first key with the second as tie-breaker; both levels persist in the saved state | local 2026-07-21T21:12 |
| Composite fields render (money/emails/phones/links/address/fullName) | local | `CONFIG_PATH=journeys/fixtures/depth.config.json npm run serve` → Vendors table + Sales reps board | money renders “€12,500” right-aligned; email/phone chips show first + “+N”; links anchor with the bare host (new tab); address one-lines “street, city”; a fullName primary titles the row link, record page and kanban card; a money rollup sums per column | local 2026-07-21T21:12 |
| Composite field editors (one-patch shaped saves) | local | open a vendor record → add an email, change the amount, set the city, change the last name | each edit saves as ONE patch (toast), survives reload, and the timeline logs readable old → new (“13750 EUR”, “…, Ghent”) | local 2026-07-21T21:12 |
| Composite validation (per-entry, field-named) | local | vendor record → type a bad email / bad URL / bad currency code into the editors | a visible inline error NAMES the field; the record keeps its old value; wrong-shaped API payloads 400 with the field named | local 2026-07-21T21:12 |
| Composite CSV flatten (shaped values in exports) | local | Vendors → select 2 rows → Export CSV | the downloaded file carries “12500 EUR”, “a; b” lists, the joined address/name — and a comma inside a URL stays one quoted column | local 2026-07-21T21:12 |
| Nav placement (config app.nav: top) | local | set `app.nav: "top"` in the config → reload (fixture: `journeys/fixtures/navtop.config.json`) | the nav renders as one horizontal top bar (brand, items with live counts, search/theme/sign-out on the right); no sidebar; clicking an item switches the list | local 2026-07-21T21:12 |
| Mobile nav drawer + full-screen peek | local | viewport ≤768px (side or top nav mode) → tap the burger | a left drawer lists objects/pages/favorites/team/sign-out; picking one navigates and closes it; an open record peek covers the full viewport; no horizontal page scroll at 390px | local 2026-07-21T21:12 |
| Possible duplicates panel (record page) | local | open a record that has a suspected twin | a "Possible duplicates" section lists each candidate WITH the matched rule ("Same name ignoring case…", "Same email"…) and announces "Review merge"; one click lands in the existing merge dialog preselected; Cancel keeps both records and leaves the pair selected | local 2026-07-21T21:12 |
| Duplicate sweep (find + review-merge) | local | any list → Find duplicates | a dialog groups suspected duplicates with member names + the rules that matched; per-group Review merge opens the merge dialog preselected; a clean object reports "No duplicates found."; nothing mutates without the merge confirmation | local 2026-07-21T21:12 |
| Relation identity (store ids, read labels) | local | depth fixture → open a vendor → pick a Sales rep → rename that rep | the picker lists fullName targets by joined name; the saved link is an ID (`_refs`); renaming the rep updates every inbound cell/section with NO sweep; the rep record shows the inverse-labeled “Vendors covered” section | local 2026-07-21T21:12 |
| Many-to-many relations (one committed diff on close) | local | vendor record → Certifications → toggle two options → Escape | toggles stay local while open; closing fires exactly ONE write with both ids; chips render both labels; reopening shows them checked; a chip’s × detaches just that link | local 2026-07-21T21:12 |
| Polymorphic relations (cross-type search, type labels) | local | vendor-note record → About → search “Atlas” | one search lists a vendor AND a rep with per-row type tags; picking each links the RIGHT identity (`_refs` carries object+id); the note lists on its target’s record under the poly field’s inverse label | local 2026-07-21T21:12 |
| Relation integrity (merge re-points ids; trash keeps, destroy severs) | local | merge two reps → trash/restore the winner → destroy a linked certification | after merge every inbound link points at the winner’s id; a trashed target keeps projecting its label and restore heals with zero writes; destroy removes the id from every pointing row | local 2026-07-21T21:12 |
| Composite-aware create dialog (shaped drafts + id-mode relations) | local | Sales reps → New → first/last + quota; Vendor notes → New → pick About | the dialog authors fullName + money as SHAPED values (code uppercased); the poly select is grouped by type and saves a typed ref that reads back as the target’s label | local 2026-07-21T21:12 |
| Relation identity replays (warehouse log, rename-safe) | mock | boot the depth fixture with the mock warehouse → label-writes + renames → restart | replayed state equals live state byte-for-byte: ids survive renames, labels re-project, a label written AFTER a rename stays dangling verbatim | local 2026-07-21T21:12 |
| Tasks page (buckets, filters, complete) | local | nav → Tasks → create tasks with due dates → complete one | tasks group into Overdue/Today/This week/Later buckets; completing one visibly moves it to Done (API stamps doneAt); the status filter narrows the visible rows | local 2026-07-21T21:12 |
| Tasks on records (link, complete, unlink) | local | open a record → Tasks card → add a task → complete it → unlink | the task appears in the record's Tasks card AND on the Tasks page with a chip back to the record; completing stamps "Task done: …" on the record timeline; unlinking removes it from the record but keeps the task | local 2026-07-21T21:12 |
| Task assignment mail | local | AUTH_MODE=accounts → create a task assigned to a signed-up user | the assignment mail ("Task assigned: …") lands in the dev outbox for the assignee — never for the actor | local 2026-07-21T21:12 |
| Panel search page (slash) | local | press `/` on any list → type a cross-object query | the panel opens on a search page with an always-focused input; results span objects with type labels; Enter pushes the hit onto the SAME panel (crumbs "Search › …") while the list behind stays untouched | local 2026-07-21T21:12 |
| Panel actions page (context actions) | local | open a record peek → the ⚡ header button (or Cmd/Ctrl+K while the panel is open) | an actions page stacks over the record listing the palette's context actions; running Favorite pins the record; "Search records" pushes the search page (3-deep crumbs); Escape walks the whole stack closed | local 2026-07-21T21:12 |
| Slash-key guard (grid + dialogs win) | local | focus a table cell, press `/`; open a dialog, press `/` | on a cell, `/` seeds the inline editor and no panel opens; with a dialog open, `/` types into the dialog; from ROW focus (navigation), `/` opens search | local 2026-07-21T21:12 |
| Runtime schema: add a field (colored select, everywhere on reload) | local | Schema page → Add field on an object → select type + colored options → Create | the commit reloads the app; the field appears on the schema page, as a table column, a filter chip, and a record-page editor; a saved value lands in the API and the timeline | local 2026-07-21T21:12 |
| Runtime schema: add an object (full CRUD + runtime relation) | local | Schema page → Add object → then Add field (relation) on it | nav grows the object; a row creates through its dialog; the runtime relation links by ID through the identity picker and projects the target's label; trash + restore round-trips | local 2026-07-21T21:12 |
| Runtime schema: retire + re-activate (data preserved) | local | Schema page → Retire on a field with data → Re-activate | the field leaves the table and record page while the API still returns its stored values and rejects writes (400); re-activation brings the values back intact | local 2026-07-21T21:12 |
| Runtime schema replays (deltas + data interleaved, restart byte-equal) | mock | mock warehouse → schema ops + dependent data writes → restart | after restart, the merged config AND every object's rows equal the pre-restart state byte-for-byte — deltas re-apply at their original position in the log | local 2026-07-21T21:12 |
| Runtime schema guard rails (every block names its reason) | local | Schema page → try a duplicate key, remove an in-use option, set unique over duplicates | each block surfaces a message NAMING the reason (the colliding key, the held option value, the duplicate values); type-change-with-data, key changes, and retiring the primary 400 with named reasons | local 2026-07-21T21:12 |
| UI gallery (catalog + live skin switching) | local | nav → Gallery → click a skin → reset | primitives, a live vendored subset, the full inventory (self-checking snapshot stamp), record-core minis and every field type render on ONE page; picking ember repaints it all instantly; reset restores the app skin; nothing persists (nx-skin-css untouched); the rest of the app is unaffected | local 2026-07-21T21:12 |
| Gallery record-core sandbox (local rows) | local | Gallery → Record core section → use the keyboard grid | the mini DataTable's full keyboard model works on LOCAL fictional rows (type-to-edit commits a visible value) with zero /api requests — the page works on an empty app | local 2026-07-21T21:12 |
| Settings tab shell | local | nav → Settings → click the About tab, then back to Writing rules | the active tab bar indicator moves and the body content swaps between the static About text and the rule list | local 2026-07-21T21:12 |
| Settings rules (EditableRuleList CRUD) | local | Settings → Writing rules → Add rule → edit it → toggle Active off → delete | @390px the add form lays out as a 2×2 grid; the new rule appears in the list, edits persist after Save, the row dims when Active is off, and deleting the last row shows the empty state | local 2026-07-21T21:12 |
| AI field enrichment (runAiTask + ThinkingDots) | mock (task, via `field.primitive.taskId`) | boot `journeys/fixtures/foundations.config.json` + `NEXUS_API_KEY` + a mock Nexus base → open a record → click the enrich (Sparkles) affordance | the affordance becomes a ThinkingDots running indicator while the AI task runs, then the field fills with the task's output (mock returns a marker); with no `taskId`/key the mock-value path is byte-unchanged | local 2026-07-21T21:12 |
| External-writer sync (RemoteStore.sync + warehouse loadSince) | mock (BigQuery seam) | boot `WAREHOUSE=bigquery` + a mock warehouse → an external writer appends a `patch` event → POST /api/sync | the externally-written value appears in the list after the sync pulls the event in (no restart) — RemoteStore.sync() + warehouse loadSince/_read end to end | local 2026-07-21T21:12 |
| Async hook cores (computeAsyncOp stall, createDebouncer coalesce) | local | node-level unit (injectable clock) — `npm run journeys` runs it | the stall flag flips only past the threshold; 3 rapid debounced triggers coalesce to ONE persist ending in `saved`. UNIT-tested (no foundations UI); VISIBLE consumer: editor autosave state + copilot poll (wave 2) | local 2026-07-21T21:12 |
| Async server helpers (emulatorChat, fireAsyncGeneration) | mock (Nexus seam) | node-level seam against a mock Nexus boundary — `npm run journeys` runs it | emulatorChat returns the agent reply + invoked tools through the session proxy; fireAsyncGeneration creates the placeholder row and fires the webhook with the created id folded in. SEAM-tested (no foundations UI); VISIBLE consumer: copilot panel + generation wizard (wave 2) | local 2026-07-21T21:12 |
| Rich-text editor (richText field) | local | Docs fixture → open a doc → type in a block → space + `/` → pick Heading 1 | the editor renders the seeded `Block[]`; `/` opens the slash menu; the paragraph becomes an `h1`; a "Saved" chip appears; the transform + text survive a reload | local 2026-07-21T21:12 |
| Rich-text editor drag-reorder | local | Docs fixture → open a doc → drag the third block's handle above the first | the block order changes (ed3 to the top) and the new order persists across a reload | local 2026-07-21T21:12 |
| Rich-text concurrent-poll safety | local | Docs fixture → open a doc → type into a block → an external writer overwrites the body + renames the record | the poll re-renders the header (renamed) but the in-progress editor edit is NOT clobbered by the polled server body | local 2026-07-21T21:12 |
| Rich-text table preview | local | Docs fixture → Docs list | the richText cell shows readable truncated prose (never `[object Object]` or raw JSON) | local 2026-07-21T21:12 |
| Wizard — config-driven multi-step block | local | Gallery → Wizard section → Open wizard → guided → step through kind/ask/notes/tags/sources → Generate draft | picking a select option auto-advances; Next disables on an empty required step; the review screen lists every answered step (incl. chips and the source URL); completing fires a toast with the collected answer and closes the modal | local 2026-07-21T21:12 |
| Advanced filters (any column · unified search box · removable chips) | local | any object list → the ONE search box → type a value → pick "Field is Value" from the dropdown; remove the chip | the same box does free-text search AND builds type-aware filters over any column; active filters show as removable chips and persist in the saved view; free-text still narrows and clears | local 2026-07-21 |
| AI copilot side-panel (docked, config-driven, Markdown replies) | mock (agent deployment, via `COPILOT_DEPLOYMENT_ID`) | add a `copilot` block to `starter.config.json` → open a record → click the Copilot toggle (or ⌘/Ctrl+I) → type a message → Send | the docked right panel slides in and pushes content; the agent reply renders as Markdown (headings/bold) with the invoked tools as chips; the per-turn context is built from the object's `contextFields`; no `copilot` block → nothing renders | local 2026-07-21T15:51 |
| AI suggestions (tracked changes) | mock | a `richText` field with `suggestTaskId` → open the record → "Request suggestions" → Accept one card, Reject another | inline tracked changes render in the editor and as rail cards; Accept folds the replacement into the block text and resolves its widget; Reject leaves the original; a config-state Pipeline marks the current stage | local 2026-07-21T15:51 |
| AI suggestions persist across reload | mock | request suggestions → Accept one → reload the record | the accepted merge is still in the text after reload and the rail shows the resolved change with an undo | local 2026-07-21T15:51 |
| Standard record layout (richText field full width) | local | an object with `recordLayout:"standard"` (default) + a `richText` field → open a record | the richText editor spans the full details column and renders as flowing text; its block is well over 200px wide (never collapsed one-letter-per-line) | local 2026-07-21T15:51 |
| Side-peek richText readable (collapse fixed) | local | a standard object → open a row in the side-peek → Request suggestions | the editor stays readable in the narrow peek and keeps its floor width even with the suggestions rail active (the editor+rail grid stacks instead of crushing the doc column) | local 2026-07-21T15:51 |
| Document record layout (wide hero editor + suggestions) | local | an object with `recordLayout:"document"` + a `richText` field → open a record | the primary richText field becomes a wide hero editor (>500px) as the main column with a compact sidebar (other fields + timeline); Request suggestions renders tracked changes beside it | local 2026-07-21T15:51 |
| Mobile bottom tab bar (config-driven, hideInNav, copilot tab) | local | viewport ≤768px → the fixed bottom bar (fixture: `journeys/fixtures/mobile.config.json`) | one tab per `config.objects` (an object flagged `hideInNav` is omitted) plus a Copilot tab when `config.copilot` is set; tapping a tab switches the list; the Copilot tab opens the dock; no horizontal scroll at 390px | local 2026-07-21T15:51 |
| Shortcuts overlay (?) + config-driven go-to chords | local | press `?` on any surface; press `g` then a mapped key | `?` opens a help modal that splits Core (shell keys) from App (the config go-to chords + new-record); Escape closes it; `g` then a `config.app.goChords` key jumps to that route | local 2026-07-21T15:51 |
| Mobile review banner (steps a record set with prev/next/act) | local | viewport ≤768px → open a record from a multi-row list | a bottom banner shows "N / M" for the set you opened; prev/next step the record (both the position and the record change); the act button promotes the current record to its full page | local 2026-07-21T15:51 |
| Guided create flow (config-driven wizard) | local | Docs (any object with `createWizard`) → New → Guided → answer title/status/body (a select auto-advances, a required step gates Next) → Create | the guided-vs-blank landing opens; walking the questions creates the record and opens it with the answered title + status and the body answer rendered as a paragraph; Blank drops to the plain dialog unchanged | local 2026-07-21T15:51 |
| Live-sync pulls external writes | local | any list → the topbar Sync button; an out-of-process writer appends a record to the warehouse → click Sync | the Sync affordance shows working dots then "synced N"; the externally-added row appears in the running list with no reload (on the in-memory app it reads "up to date") | local 2026-07-21T15:51 |
| Async generation placeholder settles | mock | an object with a `generate` config (demo: Reports) → its list → Generate | a placeholder row drops immediately (status Generating) with a working indicator; the finished record lands from the warehouse and the SAME row settles to Ready (no duplicate); the indicator clears; swap the /api/_mock/generate writeback in server/server.mjs for a real generation workflow | local 2026-07-21T15:51 |
| Rich-text save-state shows Saving then Saved | local | open a record with a `richText` field → type in the body | a subtle chip shows "Saving…" while typing then "Saved" once the debounced commit lands; nothing shows at rest | local 2026-07-21T15:51 |
| View registry (config-driven views per object) | local | boot `journeys/fixtures/view-registry.config.json` → Projects → switch tabs → reload; Field notes → timeline tab; 390px: tap Board | switcher tabs come from the installed view registry (an object offers exactly its configured views, labels from the definitions); the chosen view persists after reload; an uninstalled type shows a graceful "not installed" chip, never a crash; tabs and toolbar menus are tap-native (verified 390x664, touch) | local 2026-07-21T15:51 |
| Sheet view (spreadsheet grid) | local | boot `journeys/fixtures/grid-view.config.json` → Projects → Sheet tab → edit a cell, drag the fill handle, copy/paste a range; 390px: tap a row marker | Excel-grade editing over records: the typed value, the 3 filled cells and the pasted 2x2 region all persist after reload (one merged patch per row); the frozen primary column holds under horizontal scroll; zero rows show the designed empty state; 10k rows paint and jump-to-end inside the perf budget; tabs and markers are tap-native at 390x664 (touch), fill-handle is desktop-only by design with inline overlay editing as the mobile edit path; a selected cell keeps legible text on any skin or theme (its text flips white or black for contrast against the selection background, verified light and dark); the grid is themed to read as a native part of the app, not an embedded widget (header, cell surfaces and scrollbars match the DataTable) | local 2026-07-21T15:51 |
| Flow view (records as node graph) | local | People → Flow tab (any object: `views:[{"type":"flow"}]` + a relation field; fixture: `journeys/fixtures/flow-view.config.json`) → drag a card → reload → click a card; "via" picker on a 2-relation object; 390px: tap a card, tap zoom | records render as draggable record-card nodes with minimap + zoom controls; the configured relation draws parent→child edges (self-relation) or labeled target hubs (cross-object); a dragged position survives reload; a node click or Enter opens that record's peek; an API-created row appears as a node via the rev poll with no interaction; a missing relation degrades to a plain-language chip and zero rows to a designed empty state; on the 10k fixture the DOM stays a windowed fraction and node→peek stays responsive; tap-native at 390x664 | local 2026-07-21T15:51 |
| Whiteboard field (canvas on records) | local | Docs → open “Q3 launch plan” → the Sketch block; toggle the theme while it is open | an excalidraw canvas mounts as a full-width block (document layout after the hero; standard layout below the details, incl. inside the side peek); a live theme toggle flips the mounted canvas to dark; the shipped docs demo seeds a real scene. Mobile is tap-to-edit (see its row); inline drag-draw is desktop-first | local 2026-07-21T15:51 |
| Whiteboard draw persists (warehouse round trip) | local | fixture on `WAREHOUSE=local` → open an empty sketch → draw a rectangle | a Saving…/Saved chip appears; the element count rises through the record API; the scene survives a reload on the file-backed warehouse store | local 2026-07-21T14:47 |
| Whiteboard cell thumbnails | local | Sketches table + Boards kanban (fixture) | a seeded scene renders as a real SVG thumbnail in its table cell AND on its kanban card; an empty scene shows a subtle canvas glyph; the list page never mounts the editor | local 2026-07-21T15:51 |
| Whiteboard concurrent-poll safety | local | draw locally → an external writer replaces the scene + renames the record | the rev-poll re-renders the header (renamed) while the mounted canvas keeps the LOCAL scene — the next save writes local content, not the external overwrite | local 2026-07-21T15:51 |
| Whiteboard mobile tap-to-edit | local | 390x664 → open a sketch record → tap “Edit canvas” → draw → Done | at rest the record shows a static preview + Edit (no live canvas, no horizontal scroll); the tap opens a fullscreen overlay canvas; a drawn shape saves; Done returns to the preview | local 2026-07-21T15:51 |
| Whiteboard keyboard + announcements | local | Tab through a sketch record; draw once | the canvas block is a labeled group; Tab reaches controls inside the canvas; the save chip announces via role=status | local 2026-07-21T15:51 |
| Whiteboard thumbnails at 10k rows | local | boot `journeys/fixtures/whiteboard-perf.config.json` → Sheets | the 10k-row table first-renders windowed (DOM holds a window, not 10k rows), a visible scene cell hydrates its SVG thumbnail, and scroll reaches row 10000 | local 2026-07-21T15:51 |
| Whiteboard invalid-value state | local | seed a non-scene value into a whiteboard field → open the record | a designed invalid state explains the value (no crash, no editor); an explicit “Replace with a blank canvas” action writes an empty scene through the store; the list renders fine | local 2026-07-21T15:51 |
| Whiteboard native chrome (themed + trimmed + re-skinnable) | local | open a sketch record → inspect the toolbar; toggle theme; override the brand accent | excalidraw's toolbar island, tool buttons, popovers, panels and menus paint the `--nx-*` surfaces (not excalidraw's stock indigo/white); the library, keyboard-hint banner and help trigger are trimmed to a clean canvas; a live theme OR skin change re-derives the whole chrome with no reload | local 2026-07-21T16:20 |
| Calendar view (full fidelity) | local | Deals → Calendar tab, or the Kit demo → "Calendar view" (Sessions demo, lands on the current week), or boot `journeys/fixtures/calendar.config.json` → switch views via the picker → click an event (edit dialog) → drag-select a range → toggle all-day → delete via the dialog; 390px: tap an event (edit sheet), tap a day | the picker offers every enabled view (month · week/day as day-grid or hourly time-grid · listWeek/listMonth agenda · multiMonthYear) and the choice persists across reload; events carry the color field's own option palette; clicking (or Enter) opens a quick edit dialog (title/dates/all-day toggle/color/other fields) that saves through the store, opens the full record in the peek, or deletes behind an inline confirm (soft-delete to trash); a drag moves the record's date (toast "Saved", survives reload) and a drag-select creates a prefilled range; a recurrence text field (RRULE) expands into occurrences across views; the time-grid day/week opens at the working hour with all 24 hours reachable by scroll, drags/creates snap to precise 15-minute times, events resize from either edge and lay out side by side when concurrent; week numbers, the now line, business-hours shading, first day and slot size honor config; an object with no date field degrades to an explanatory chip; a 10k-row object stays smooth (dayMaxEvents bounds the DOM); on mobile the grid swaps for a tappable agenda list, day rows create, event rows open the edit sheet [drag-reschedule + the view picker are desktop-only: mobile edits via the sheet] | local 2026-07-21T15:51 |
| Spreadsheet page (full Univer workbook) | local | nav → Spreadsheet (`#/p/spreadsheet`) → type `=SUM(...)` in a cell, right-click a column header then Insert cols right, Bold a cell from the toolbar, flip the theme; 390px: tap a cell and edit | a full Excel-grade workbook renders AS the page — no card frame, no extra header strip: one single-row toolbar (`ribbonType: "simple"`, every ribbon command reachable through the overflow menu) with the page's own save state + reset + clear riding its right end, then the formula bar, then the grid bleeding to the content edges (sheet tabs + zoom on the bottom edge). A typed `=SUM` computes live (E7=76900) and persists through reload; an inserted column shifts the data right and persists; a bolded cell keeps its style; ALL vendor chrome (toolbar, formula bar, name box, sheet tabs, zoom, context menus and portaled popups) resolves `--nx-*` tokens in light AND dark — flipping `data-theme` live re-skins chrome + canvas with no reload, and a skin landing re-derives both (the canvas theme stays light-anchored; Univer inverts it for dark); no saved workbook shows the designed empty state with a Create action; a 10k-row workbook paints inside the perf budget (Univer virtualizes); at 390px the toolbar collapses into its overflow menu, the compact cluster fits, and a tapped cell edits via the on-canvas editor. Free surface: the whole workbook persists as one app_state snapshot (`workbook:spreadsheet`), not record data | local 2026-07-21T21:12 |
| Gallery view (cover-card masonry) | local | boot `journeys/fixtures/gallery-form.config.json` → Shots (or the starter's Showcase demo via the Kit demo page); 390px: tap a card; `#/o/bulk` for 10k | cover cards pack into masonry columns with colored meta chips; a coverless/broken row shows an initials placeholder; click/tap/Enter opens the record peek (`?peek=` in the URL); a 10k object paints a WINDOWED slice (DOM stays bounded through deep scroll) and stays clickable; empty state offers a create CTA | local 2026-07-21T15:53 |
| Form view (config-driven intake) | local | same fixture → Leads (defaultView form; or People → Form tab); 390px: touch submit | the configured field subset renders in configured order; submitting with a missing required shows an inline error NAMING the field and focuses it (keyboard path: type + Enter); a valid submit (WAREHOUSE=local fixture) shows the success state, "Create another" resets, and the row is visible in the Table view with its typed number intact; no-create-permission renders a designed state | local 2026-07-21T15:53 |
| Unified field editors (draft coercion) | local | same fixture → Leads → New lead → fill Name + a numeric Size → Create | one field-type registry backs the create dialog, form view and wizard coercion: a TYPED number now creates successfully (previously the dialog sent a string and the server 400d "must be a number") and persists as a number; select options render their configured labels everywhere | local 2026-07-21T15:53 |
| Map view (records on a map) | local | Kit demo → "Open the map demo" (or `#/o/demo_places`) → click a pin → Open; type a no-match search; Tab to a pin → Enter; 390px: tap a pin → Open | records with valid lat/lng render as token pins (tinted by the select-option palette); a corner chip counts records WITHOUT coordinates; a pin click opens a record-card popup (title + two fields) whose Open lands on the record peek; the keyboard path (focused pin → Enter → Enter) reaches the same peek; with tile hosts unreachable the map degrades to a token canvas + "Map tiles unavailable" chip and every pin/popup keeps working; a record created by an external writer appears as a pin via the rev poll with no reload; the designed empty state shows when a filter clears the map; fully touch-operable at 390x664 (tap pin → popup → Open) | local 2026-07-21T21:10 |
| Map view clustering at scale (10k) | local | boot `journeys/fixtures/map-perf.config.json` → the journey seeds 10,000 rows via the import API → `#/o/places_perf` | past 25 located records the view switches to GL clusters (container mirrors GL state on `data-map-mode`/`data-map-clusters`); with 10k rows the map reaches ready under 15s; clicking a cluster zooms toward its expansion; zooming out re-forms clusters; an object whose `views` declare `map` without number fields renders the graceful misconfig chip naming the gap | local 2026-07-21T21:10 |
| Gallery depth (group/sort/card fields) | local | boot `journeys/fixtures/gallery-form-depth.config.json` → Shots (toolbar Group/Sort pickers); 390px; `#/o/bulk` for 10k | cards split into collapsible sections with per-group counts (collapse persists on reload); the toolbar sort reorders cards and persists; configured `cardFields` render in order through the field registry (selects as colored chips); single column at 390px; a 10k object stays windowed | pending |
| Gallery depth (cover fit/source) | local | same fixture → Moods | coverFit "contain" vs "cover" visibly differ; a links/array cover uses the FIRST image in the field | pending |
| Gallery depth (card selection) | local | same fixture → Shots → hover a card → its checkbox | clicking the checkbox selects the card into the shared bulk bar (data-selected) | pending |
| Form depth (sections + conditional required) | local | same fixture → Leads | fields render in labeled Contact/Company sections; choosing the trigger value makes a field required (the asterisk appears); a failed submit shows the jump-to-field error summary; filling the field submits | pending |
