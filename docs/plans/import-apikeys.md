# Lane: import-apikeys (T0)

## Feature A — CSV import
- Route: `POST /api/objects/:key/import` `{ rows: [{fieldKey: value}...], preview? }` — in the objects block of `server/server.mjs`, gated `deny("create")`, cap 2000. Per row (sequential): coerce types from field config (number/currency/rating/boolean; empty string → omit; unknown keys dropped) → missing primary → `failed` → unique value consumed EARLIER IN THIS FILE (per-request seen-set, marked on created/restored) → `skipped` (duplicate within this file — keeps preview and run verdicts identical) → matching a LIVE row → `skipped` (duplicate) → matching a TRASHED row → validate (zombie id as excludeId) + `restoreRow` + `patch` → `restored` (emits `<key>.restored`) → else validate → `failed(reason)` | `create` → `created` (emits `<key>.created`). `preview:true` = same classification, zero mutation.
- Response: `{ results: [{index, verdict, id?, reason?}], totals: {created,restored,skipped,failed} }`.
- Store: NO new store op — loop lives in the route over existing logged ops (`create`/`restoreRow`/`patch`), replay stays deterministic. Events only fan out to subscribed webhooks (emitEvent no-ops otherwise).
- Client: CSV parser in `ObjectView.tsx` (quoted fields, "" escapes, newlines-in-quotes, CRLF; no deps). Wizard dialog steps input → map → preview(5 rows via preview call) → run (chunks of 200, cancel ref between chunks, N/M progress — bulk-edit pattern) → summary + failed-rows CSV download (original columns + reason).
- Testids: `import-open` (toolbar, canCreate), `import-text/file/next/back`, `import-map-<colIdx>`, `import-preview`, `import-run/cancel/summary/failed-csv/close`. i18n: `import.*` keys appended to `i18n.ts` (append-only hunk).

## Feature B — API keys
- `server/apikeys.mjs` (new): `FEATURE_APIKEYS` flag (read from `process.env` — env.mjs is frozen; 2-line env.mjs alternative if preferred), sha256, header parse (`x-api-key` | `Authorization: Bearer`, `nak_` prefix only), `resolveApiKey`, `handleApiKeys` routes: GET `/api/apikeys` (safe fields), POST (name + role∈viewer|member|admin → secret `nak_`+32hex generated IN the route → 201 carries the secret ONCE), POST `/:id/revoke`. Management gated role owner|admin.
- Store (additive): `apiKeyAdd({name,role,prefix,last4,hash})` (id/createdAt from store), `apiKeyRevoke(id)` (sets revokedAt) → both in `LOGGED_OPS`. `last4` stored alongside `prefix` (hash is one-way; list shows `nak_ab12…wxyz`).
- Auth wiring in `server.mjs handler()` (additive; `auth.mjs` untouched): resolve key BEFORE `gate` — invalid/revoked → 401; valid → bypass session gate, `api()` gets the key and role = key.role (else existing logic). Works with auth off (a key still scopes DOWN to its role). Team-scoped objects stay session-member-only (keys have no membership) — documented.
- Page `src/app/pages/ApiKeys.tsx` + registry row (`key:"apikeys"` → nav auto-hides on `features.apikeys===false`). Role visibility: nav filter is feature-only and App.tsx is FROZEN → page self-guards via `/api/auth/me` (non-admin sees a notice; server 403s regardless). Secret-once copy box `data-testid="apikey-secret"`; revoke behind AlertDialog. `api.ts`: me/apiKeys/apiKeyCreate/apiKeyRevoke/importRows.

## Journeys (marker `// --- lane: import-apikeys`, ports 5100–5149) + docs
- `import-wizard` (main app, companies: 5 fictional rows, one odd header → manual map; cleanup delete+destroy) · `import-dedupe-restore` (depth fixture :5100 — trash ve_1/V-001 → CSV V-001+V-999 → "1 restored, 1 created", original id kept) · `import-errors` (main app, people: bad email + missing primary → reasons + failed CSV parsed) · `apikey-scoping` (:5110, accounts + NEW fixture `apikeys.config.json` with a `viewer:["view"]` permissions row — perms fixture grants viewer nothing; owner = signup + team create; key requests from a cookie-less context; read 200 → PATCH 403 → revoke → 401).
- Manifest: 2 rows — "CSV import wizard" (3 journeys share the feature string) + "API keys (role-scoped access)". RECIPES: "Let another system call the app" (create → curl → scoping paragraph) + a 3-line CSV-import recipe. DATA-MODEL untouched (no config surface).
